---
title: Core Concepts
description: The mental model behind taskflow's declarative DAG runtime.
---

This chapter explains the stable execution model beneath every taskflow. It does not enumerate every field — use the [Syntax Reference](/en/docs/syntax) for that — and it does not cover the 0.2 compiler surfaces — use [New in 0.2](/en/docs/compiler-runtime).

A taskflow rests on six ideas:

- work is a **graph**, not an implied sequence;
- twelve **phase types** build the graph;
- data moves through explicit **interpolation**;
- the graph is **verified** before token spend;
- intermediate output and filesystem work can be **isolated**;
- persisted state lets a run **resume** across sessions.

## Suggested reading order

<Steps>
  <Step>
    **[The DAG Model](/en/docs/concepts/dag)** — `dependsOn` creates edges; array order does not.
  </Step>
  <Step>
    **[Phase Types](/en/docs/concepts/phases)** — the twelve building blocks and when to use them.
  </Step>
  <Step>
    **[Interpolation](/en/docs/concepts/interpolation)** — how values move between phases without an extra model call.
  </Step>
  <Step>
    **[Verification](/en/docs/concepts/verification)** — cycles, dead ends, references, and budget checks before execution.
  </Step>
  <Step>
    **[Context Isolation](/en/docs/concepts/context-isolation)** and **[Workspace Isolation](/en/docs/concepts/workspace-isolation)** — keep transcripts and file changes inside controlled boundaries.
  </Step>
  <Step>
    **[Shared Context Tree](/en/docs/concepts/shared-context-tree)** — coordinate isolated agents without dumping every transcript into the host.
  </Step>
  <Step>
    **[Resume](/en/docs/concepts/resume)** — continue incomplete work and reuse persisted phase results.
  </Step>
</Steps>

<Callout type="info">
  Replay and incremental recompute build on this model, but are documented as 0.2 runtime capabilities in [New in 0.2](/en/docs/compiler-runtime).
</Callout>

## The concepts

<Cards>
  <Card title="The DAG Model" href="/en/docs/concepts/dag">
    How task nodes and explicit dependencies form the graph.
  </Card>
  <Card title="Phase Types" href="/en/docs/concepts/phases">
    The twelve execution and control building blocks.
  </Card>
  <Card title="Interpolation" href="/en/docs/concepts/interpolation">
    Deterministic data flow between phases.
  </Card>
  <Card title="Verification" href="/en/docs/concepts/verification">
    Catch structural errors before tokens are spent.
  </Card>
  <Card title="Context Isolation" href="/en/docs/concepts/context-isolation">
    Why only the selected final result reaches the host conversation.
  </Card>
  <Card title="Workspace Isolation" href="/en/docs/concepts/workspace-isolation">
    Run file-changing work in inherited, temp, dedicated, or worktree scopes.
  </Card>
  <Card title="Shared Context Tree" href="/en/docs/concepts/shared-context-tree">
    Coordinate agents through explicit shared context.
  </Card>
  <Card title="Resume" href="/en/docs/concepts/resume">
    Continue persisted runs across sessions.
  </Card>
</Cards>
