---
title: Reference
description: Quick reference for shorthand and commands.
---

The Reference pages are *lookup material*. They are not a tutorial — read them when you already know what you want and just need the exact shape. For the why behind any of this, see [Concepts](/en/docs/concepts); for exhaustive field-by-field detail, see the [Syntax Reference](/en/docs/syntax).

Two pages live here:

- **[Shorthand](/en/docs/reference/shorthand)** — the `task`, `tasks`, and `chain` shortcuts, and the exact full-flow JSON each one expands into.
- **[Commands](/en/docs/reference/commands)** — every Pi `/tf` command and every Codex / Claude / OpenCode / Grok MCP tool, grouped by purpose, with a worked example session.

<Callout type="info">
  On Pi, saved flows become slash-command shortcuts automatically: `/tf:review-changes` is the same as `/tf run review-changes`. On MCP hosts, you run a saved flow by name through `taskflow_run`.
</Callout>

## The pages

<Cards>
  <Card title="Shorthand" href="/en/docs/reference/shorthand">
    `task`, `tasks`, `chain` — the quick-delegation shortcuts.
  </Card>
  <Card title="Commands" href="/en/docs/reference/commands">
    Pi `/tf` commands and Codex / Claude / OpenCode / Grok MCP tools.
  </Card>
</Cards>

- **[TypeScript DSL](/en/docs/reference/typescript-dsl)** — compile-time `.tf.ts` → Taskflow JSON (`taskflow-dsl`).
