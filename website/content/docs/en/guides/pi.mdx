---
title: Using taskflow on Pi
description: Install, run, save, and resume taskflows on the Pi coding agent.
---

# Using taskflow on Pi

<Callout type="info">
  Install once, then describe flows in natural language or run saved flows by name.
</Callout>

## Install

```bash title="Install pi-taskflow"
pi install npm:pi-taskflow
```

## Run

In a Pi session, describe the work:

<Callout type="tip">
  Try: *"Run a chain: first explore the auth flow, then summarize the findings."*
</Callout>

The model calls the `taskflow` tool automatically.

## Save a flow

After a run, say **"save it"** and give it a name. It becomes a one-word command:

```bash title="Run a saved flow"
/tf:audit-auth dir=src
```

## Commands

| Command | Description |
|---|---|
| `/tf list` | List saved flows. |
| `/tf run <name> [args]` | Run a saved flow. |
| `/tf show <name>` | Print a flow definition. |
| `/tf verify <name>` | Static-check a flow. |
| `/tf compile <name>` | Render a Mermaid diagram. |
| `/tf runs` | Browse run history. |
| `/tf resume <runId>` | Continue a paused/failed run. |
| `/tf init` | Configure model roles. |
| `/tf:<name> [args]` | Shortcut. |

## Model roles

Run `/tf init` to map built-in agent roles to your models.

<Callout type="tip">
  Roles are referenced with `{{role}}` syntax inside agent definitions and phase configs.
</Callout>

| Role | Purpose |
|---|---|
| `{{fast}}` | cheap & quick |
| `{{strong}}` | balanced planning/review |
| `{{thinker}}` | deep analysis |
| `{{arbiter}}` | final judgment |
| `{{vision}}` | multimodal |
| `{{reasoner}}` | cautious reasoning |

## Next

<Cards>
  <Card title="Getting Started" href="/en/docs/getting-started">
    Run your first taskflow.
  </Card>
  <Card title="Using taskflow on Codex" href="/en/docs/guides/codex">
    Use taskflow with OpenAI Codex.
  </Card>
</Cards>
