# pnpm workspace settings. Workspace packages are symlinked automatically; this
# just makes the link strategy explicit and keeps node_modules strict (each pkg
# sees only its declared deps — surfaces phantom deps at install time).
link-workspace-packages=true
prefer-workspace-packages=true
save-exact=false
