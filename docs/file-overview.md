# File Overview

| File | Exports |
|------|---------|
| `a.ts` | `alpha` (`1`) |
| `b.ts` | `beta()` (returns `2`) |

Two files, two exports — a module boundary with one constant and one factory function.
