# 0.3.0 Trusted Effects — release plan (human gate)

## Current state

| Item | Value |
|------|--------|
| Branch | `rc/0.3.0-trusted-effects` |
| Code candidate | `96a32e8` (fix(store): harden project .pi discovery against tmp and home roots) |
| Highest proven | **L2** (local RC pipeline green at `96a32e8`: typecheck PASS; full unit suite 2202 tests → 2198 pass / 0 fail / 4 skipped; build PASS; test:pack 9 pkgs PASS; built Codex MCP e2e 16/16; audit --prod clean) |
| Remote candidate | **not pushed** — no gh/SSH GitHub auth on this host; exact-SHA remote CI still open (historical: Draft PR #117 run 31167592775 green on `1478510f`) |
| L4 live-host | **NOT_RUN** on `96a32e8` (prior-candidate historical evidence only) |
| Package versions on npm | still **0.2.8** (0.3.0 unpublished) |
| Tag | **none** |

## Human checklist for L5 → L6

1. Review scoreboard: `docs/internal/0.3.0-ga-scoreboard.md` (all 8 deliverables **pass**, L1/L2 **pass**, L4 **NOT_RUN** on current SHA).
2. Run locally (or confirm the RC logs at `/tmp/taskflow-03-rc-final/`):
   ```bash
   pnpm run typecheck
   pnpm test
   pnpm run build
   pnpm run test:pack
   pnpm run test:e2e-codex-mcp-full
   pnpm audit --prod
   ```
3. **Push `rc/0.3.0-trusted-effects` at exact candidate SHA `96a32e8`** (requires GitHub auth — gh/SSH both fail on this host) and confirm exact-SHA remote CI green.
4. Bump workspace package versions to `0.3.0` (root + publishable packages) via existing release scripts (`RELEASE.md`).
5. Move CHANGELOG `## [0.3.0] — Unreleased` → dated release section.
6. Tag `v0.3.0` and publish **only with explicit human authority**.
7. Update scoreboard L5/L6 to PASS with tag URL + publish receipt.

## Agent stop line

Agents **must not** tag or npm publish. Stop at candidate + truthful scoreboard (L5/L6 FAIL until step 6–7).
