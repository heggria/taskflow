# 0.3.0 Trusted Effects — release plan (human gate)

## Current state

| Item | Value |
|------|--------|
| Branch | `rc/0.3.0-trusted-effects` |
| Code candidate | `e7c5e31` (fix(test): Windows child-process store import portability — test-harness only, no product code change; product candidate `96a32e8` store-discovery hardening unchanged) |
| Highest proven | **L4 real-environment (built-MCP fixture)** with **exact-SHA remote CI green** (run 31460133496 on PR #122 head `e7c5e31`: test node 22/24, e2e codex MCP network-free incl. built-dist full, build, packed consumer 9 pkgs, website export, process supervisor 3 OSes, CodeQL; local `e7c5e31` typecheck + 2198/0/4; RC pipeline at `6071fb2` adds audit clean + built Codex MCP 16/16) |
| Remote candidate | **GREEN exact-SHA CI** — Draft PR #122 head `e7c5e31`, run https://github.com/heggria/taskflow/actions/runs/31460133496 conclusion success, full matrix incl. windows process-supervisor `store.test.ts` PASS (prior red run 31456747148 on `0f9cf16` fixed by the Windows test-harness portability fix in `e7c5e31`) |
| L4 real-environment | **PASS (built-MCP fixture)** — built dist Codex MCP e2e 16/16 incl. TE fixture (`codex-mcp-full.log`, evidence SHA `6071fb2`); CI e2e job re-runs `test:e2e-codex-mcp-full` at exact SHA `e7c5e31`; live `test:e2e-codex` NOT rerun on current tip (prior-candidate historical A→B→C only) |
| Package versions on npm | still **0.2.8** (0.3.0 unpublished) |
| Tag | **none** |

## Human checklist for L5 → L6

1. Review scoreboard: `docs/internal/0.3.0-ga-scoreboard.md` (all 8 deliverables **pass**, L1/L2 **pass**, L4 **PASS** for the built-MCP fixture with the live-Codex-CLI leg still open on current SHA).
2. Run locally (or confirm the RC logs at `/tmp/taskflow-03-rc-final/`):
   ```bash
   pnpm run typecheck
   pnpm test
   pnpm run build
   pnpm run test:pack
   pnpm run test:e2e-codex-mcp-full
   pnpm audit --prod
   ```
3. **Exact-SHA remote CI: DONE** — Draft PR #122 is open (head `rc/0.3.0-trusted-effects` @ `e7c5e31`) and run 31460133496 is green across the full matrix incl. windows process-supervisor (`gh` is authenticated on this host; the PR was opened/updated from `docs/internal/pr-0.3.0-trusted-effects.md`). If the rc tip moves again, re-verify the PR head before relying on this.
4. Bump workspace package versions to `0.3.0` (root + publishable packages) via existing release scripts (`RELEASE.md`).
5. Move CHANGELOG `## [0.3.0] — Unreleased` → dated release section.
6. Tag `v0.3.0` and publish **only with explicit human authority**.
7. Update scoreboard L5/L6 to PASS with tag URL + publish receipt.

## Agent stop line

Agents **must not** tag or npm publish. Stop at candidate + truthful scoreboard (L5/L6 FAIL until step 6–7).
