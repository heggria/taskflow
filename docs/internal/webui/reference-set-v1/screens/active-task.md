# Active Task

Status: rendered with automated checks; human approval is still pending.

The fixture is driven by `WebTaskProjectionInput` and the canonical server Task
projection. Simple and Pro must share its headline, detail, progress, active
step, verification state, and primary navigation action.

Expected comprehension: the user can identify what is running and whether they
need to do anything now.
