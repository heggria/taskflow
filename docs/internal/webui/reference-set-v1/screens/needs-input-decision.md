# Needs Your Input

Status: rendered with automated checks; human approval is still pending.

The fixture has both current-principal `approve` and `reject` actions with live
CAS inputs. The decision projection supplies the question, impact, equal-weight
labels, and both consequences. Workflow prose is quoted context only.

Expected comprehension: the user can state what each choice will cause and
that neither choice is preselected or recommended.
