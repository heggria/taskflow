# Pro Evidence / Receipt

Status: rendered with automated checks; human approval is still pending.

The fixture binds immutable Receipt identity to current verification and the
first bounded event-manifest page. Complete evidence continues through the
Receipt and artifact endpoints.

Expected comprehension: the user can say what is immutable evidence, what was
checked now, that one page is not the entire manifest, and that the local
export check proves JSON completeness against the Receipt rather than
replacing the authoritative evidence result.
