# Settings

Status: rendered with automated checks; human approval is still pending.

The fixture is the bootstrap contract for Simple/Pro, theme, locale, compatible
protocol range, session expiry, features, and commands. UI mode and locale are
view state; neither changes ControlStore authority or returned resource truth.

Expected comprehension: the user can switch presentation mode without
believing they changed how a Task executes.
