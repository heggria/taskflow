# 0.3.0 Closed-Loop GA Traceability Matrix

> Living matrix: **full** §23 + D1–D38 + P1–P16. No “selected MUST” shortcuts for GA claims.  
> **Every PASS must link a behavior test or runtime proof.** PARTIAL/FAIL stay explicit.  
> Branch: `feat/0.3.0`. Tip evidence SHA is stamped in `{SCRATCH}/ga-evidence-report.md`.  
> Do **not** claim closed-loop GA while any MUST row is FAIL/PARTIAL, remote CI is unproven, or dual paths remain.
> Active closure protocol: [`0.3.0-ga-closure-goal.md`](./0.3.0-ga-closure-goal.md).
>
> **P14 supersession notice (2026-07-27):** every historical Round below that
> requires an external freshness witness, non-rollbackable root, or externally
> authenticated repair before 0.3 GA is superseded. 0.3 explicitly excludes
> only coherent restoration of every locally consulted authority byte to one
> older internally valid snapshot. Partial/mixed rollback, corruption/loss,
> half-commit, crash, stale authority, fencing, path replacement, compaction,
> migration, and recovery races remain in scope.

**Legend:** PASS | PARTIAL | FAIL | N/A | DEFER

**Evidence root:** plan `{SCRATCH}` = implementer scratch (logs + sha256 in `ga-evidence-report.md`).

---

## Dual-path inventory (freeze)

| Surface | Path today | ControlStore? | Status |
|---------|------------|---------------|--------|
| MCP foreground `taskflow_run` | `tryControlPlaneRun` → ControlHost `schedulePhases` + script/llm providers | Yes (standalone host, **skipSingleton**) | **PARTIAL** — not daemon singleton writer |
| MCP background | current `feat/0.3.0`: default rejects before legacy detached launch; explicit `TASKFLOW_CONTROL_PLANE=0|false|off|no` uses 0.2 `launchMcpBackgroundRun` / `.pi` | No daemon-backed lifecycle (default creates no background run; opt-out uses `.pi`) | **FAIL** — default silent fallback is removed, but no shared ControlStore/journal/Receipt lifecycle exists |
| MCP when `TASKFLOW_CONTROL_PLANE=0` | `executeTaskflow` 0.2 | No | **intentional opt-out** (PARTIAL for GA) |
| Pi adapter run | default `run`, `resume`, live `recompute`, detached execution, saved-flow shortcuts, and run-history resume deny before the legacy RunState lifecycle; explicit `TASKFLOW_CONTROL_PLANE=0|false|off|no` reaches local `executeTaskflow` / `.pi` | No ControlStore client or daemon lifecycle (dry-run/read-only legacy observation remains non-unified) | **FAIL** — default dual-store effects are stopped, but no shared command/run/Receipt lifecycle exists |
| CLI standalone / writer | `bootstrapControl` → local ControlHost | Yes | **PASS** (local) |
| CLI attach (auto) | `controlClientRpc` UDS → daemon writer | Yes (via daemon) | **PASS** (local; `uds-hardening`) |
| taskflowd | singleton + UDS + ControlHost mounts | Yes | **PASS** (local) |
| approve()/edit() | legacy parked Run → `TF_FEATURE_REQUIRED`; native script approve → journaled cursor/re-reserve/provider terminal | Yes, narrow native proof | **FAIL** mandate 6 lifecycle truth — native approve is only a script-provider slice; edit and the remaining lifecycle/provider/entrypoint cases are not closed |

---

## §23 GA checklist (complete)

| # | Item | Impl | Behavior test / proof | Evidence | Status |
|---|------|------|----------------------|----------|--------|
| 23.1 | Fresh install, default auto, one run, no manual daemon config | `bootstrap.ts` controlMode auto | `closed-loop.test.ts`, cli tests | unit/control logs | **PARTIAL** — auto works; MCP still standalone skipSingleton |
| 23.2 | Concurrent client start → single writer / attach | hard-link singleton + durable epoch + process-held mutation capability | daemon concurrent; `modes-and-stress` fresh-start tests; Round 64 copied-record/raw-global/alias regressions | local control/daemon | **PARTIAL** — one process-held capability now gates singleton release and global coordinator mutation on the actual main worktree; copied lock JSON, forged objects, raw global callers, and a canonical-global `baseDir` alias are denied, while a real capability derives the reservation epoch. Fresh 24-process and historical 32×100 acquisition tests remain scoped local evidence. A crashed authority lock deliberately fails closed with no automatic recovery; pathname replacement, stale-owner takeover, holder-authenticated UDS, Windows, all-entrypoint fences, and a current 32×100 stale/restart packet remain unproved. |
| 23.3 | Stale socket recovery | uds-server unlink dead sock | daemon UDS tests | daemon tests | **PASS** (local) |
| 23.4 | Per-project store; registry multi-mount; standalone+daemon same store | project-store, registry | closed-loop + daemon mount | control/daemon | **PARTIAL** — same schema; MCP not always same writer |
| 23.5 | Registry wiped → reopen restores projectId/domainId from header | identity + header | closed-loop identity | control tests | **PASS** (local) |
| 23.6 | maxActiveRuns slots≡1; N+1 capacity | coordinator + queued/rebound AdmissionIntent slice | `closed-loop` P16 real-Script r1→r2→r3 expiry/restart; `stale-lock` capability-derived epoch; `store-recovery` partial-loss tests | local only | **FAIL** — elapsed pre-commit leases preserve retry-same-command/no-marker evidence and a fresh rebind produces one dispatch; admission-bound reservations cannot use the unbound release path. Round 64 makes normal global mutations capability-held and derives reservation epochs under the live fence. This is still not capacity closure: coordinator project binding and release predicates remain caller-shaped, project/coordinator commitment is not atomic or independently verifiable, `SlotCommitted → first dispatch` has no restart owner, and retention/backpressure/current pressure proof is absent. Coherent rollback of every local authority byte is excluded by the P14 scope decision; partial loss/corruption remains in scope. |
| 23.7 | committed/orphan-suspect release only D37 | coordinator normalRelease/forceRelease + admission-bound unbound-release guard | `closed-loop`, `force-release-approval`, Round 57 admission-release guard | local only | **FAIL** — a raw caller can no longer use the *unbound* release API on a reservation carrying `admissionId`; that removes one pre-commit interference path. `normalRelease` still trusts caller-provided `ReleaseContext` booleans, force release still accepts only a string principal, and neither is capability-bound to project terminal/parked evidence plus provider-quiescence proof. |
| 23.8 | reconcile exhausted → unknown + needs-operator + no Receipt | reconcile + control-host | closed-loop reconcile | control | **PASS** (local) |
| 23.9 | wait snapshot + TF_RECONCILE_REQUIRED | control-host.wait | closed-loop wait | control | **PASS** (local) |
| 23.10 | CoordinatorCommandRecord setMax/force | coordinator | `store-recovery`, `force-release-approval` | control | **PARTIAL** — command authority is now state-only (no writable sidecar), validated before save, unique/contiguous/idempotent and principal-bound locally; actual operator authentication, project-journal binding, retention, and crash/recovery authority remain unproved. |
| 23.11 | Approval parked releases slot; approve → queued+re-reserve; CAS | journaled native continuation + legacy denial | approval-cas; approval-continuation; Round 57/58 real-Script TTL review | control | **FAIL** — a script-only native happy path proves parked checkpoint → restart → CAS winner → re-reserve → downstream provider terminal → Receipt, but it is not the generic admission saga. Round 58 makes the forced-TTL `running/admitted + expired reservation` window return `TF_RECONCILE_REQUIRED/operator/none` on both the initial approval and same-command retry, with no downstream Script intent/marker. That is an honest stop, not recovery: native approval still reserves without `admissionId`, writes project admission before coordinator commit, cannot rebind or resume the ownerless continuation, and leaves edit/reject/expire/cancel, non-script providers, crash boundaries, and host-entry parity open. |
| 23.12 | Command idempotency; disclosure re-auth; ArtifactRef authz | queued/rebound AdmissionIntent + claimCommand; artifact-uri | `closed-loop` P16 real-Script r1→r2→r3 restart; Round 56–58 crash review | control | **FAIL** — queued identity and the narrow pre-provider expiry recovery are locally stable: the same command/run survives two elapsed leases with no double dispatch. Round 58 stops `executing` with no active attempt **or only schema-valid `prepared`** state from being disclosed as `ok:true`; it returns `TF_RECONCILE_REQUIRED/operator/unknown` until a durable intent-or-later state exists. It is not closed-loop idempotency: after `SlotCommitted` before first dispatch, same-command retry still returns reconcile without a scheduler owner; native approval remains a separate ownerless admission path; wire migration and all-entrypoint evidence are absent. |
| 23.13 | Receipt event manifest; assurance when used | issueReceipt | p9 D21 route; ga-samples | SCRATCH samples | **PARTIAL** — no hardcoded artifactIntegrity ok; provenance incomplete |
| 23.14 | No DomainTransfer; no federated multi-store | surface | p-adr inventory | p-adr test | **PASS** |
| 23.15 | Public-surface goldens; cancelled; parked stage | types + goldens | wire-types; public surface | unit | **PARTIAL** — D21 dual path |
| 23.16 | P1–P16 ADRs present for shipped wire | docs/internal/p-adrs | p-adr-inventory.test.ts | control | **PASS** (docs present) |

---

## D1–D38 (complete)

| ID | Topic | Impl | Behavior test / proof | Status |
|----|-------|------|----------------------|--------|
| D1 | CACP | architecture | RFC + control packages | **PASS** (design shipped) |
| D2 | Single scheduler | phase-scheduler + residual 0.2 | phase-scheduler-adversarial; Pi still 0.2 | **PARTIAL** |
| D3 | Planes Intent…Ledger | linker + store + host | closed-loop | **PARTIAL** |
| D4 | Entity set | types.ts + stores | wire-types | **PARTIAL** — NodeInstance/Attempt not fully first-class on disk |
| D5 | controlMode auto/coord/standalone | bootstrap | closed-loop modes; coordinated fail-closed | **PARTIAL** — UDS attach incomplete for all clients |
| D6 | Scoped authority | project vs coordinator vs registry | closed-loop attach byte/derived-state tests; real-script authority/fence tests; `script-provider` cancel tests | **PARTIAL** — singleton attach now opens store/registry read-only and refuses repair-needed derived state rather than rebuilding it. The scheduler makes a fresh host authority check after durable intent and passes a synchronous fence to ScriptExecutionProvider, which holds its idempotency-reserve → OS-spawn → provider-job-ack critical section under that fence. Round 35 adds a narrow cancellation fence: a CAS-backed durable cancel intent and `signalling` boundary precede Script provider access, and an ordinary inherited group plus an intentionally detached descendant both remain `unknown/reconciling` rather than falsely terminal. The specific Script slices are covered locally, but generic/remote providers can ignore the optional contracts, the post-submit acknowledgement/recovery path is still not fenced end-to-end, and no public cancellation-reconciliation lifecycle exists. |
| D7 | projectId + directoryBinding | identity.ts | p1-semantics identity | **PASS** (local) |
| D8 | Thin MCP + CLI | mcp-route, cli | mcp-bind; cli tests | **PARTIAL** — MCP skipSingleton |
| D9 | Async ExecutionProvider | script + host-llm | script-provider; host-llm; adversarial mixed | **PARTIAL** — 12 phase kinds incomplete |
| D10 | Policy deny/sub/attenuate | policy.ts | p1-semantics policy | **PASS** (local) |
| D11 | Unsupported sandbox fail-closed | grants/enforcement | p8 grants tests | **PASS** (local) |
| D12 | Migration tiered; no DomainTransfer | rollback tiers P10 | p10 tests | **PASS** (local design) |
| D13 | ControlHost + journal + BoundPlan + CLI | packages | closed-loop + cli | **PASS** (local) |
| D14 | Workspace capability | workspace capability RFC, partially shipped | workspace RFC + current surface inventory | **PARTIAL** — the normative 0.3 RFC includes this capability, but implementation and behavior proof are incomplete; it cannot be deferred without an explicit scope-freeze decision |
| D15 | OS principal; mcp:* weak | connection-bound UDS label; no OS/adapter authentication | uds-hardening principal-switch adversarial test | **PARTIAL** — a single UDS connection can no longer replace its hello label in an RPC, but the hello label itself is client-asserted; no peer-credential or adapter-credential proof exists |
| D16 | at-least-once + idempotent submit + reconcile | queued/rebound AdmissionIntent + scheduler idempotency scope | `closed-loop` real-Script r1→r2→r3 expiry/restart; Round 56/57 finalize crash repro | **FAIL** — admission ID scopes new provider keys; a real Script proof now covers repeated pre-commit expiry and shows no pre-commit marker or double dispatch. No unique recovery owner covers finalize/first-intent/executing crashes, project/coordinator split-brain, generic concurrent writers, or all provider/entrypoint semantics. |
| D17 | BoundPlan + BoundFragment | linker; bound-fragment | bound-fragment tests | **PASS** (local) |
| D18 | One ControlHost semantics all modes | control-host + bootstrap | dual-path inventory; Pi control-gate adversarial test | **PARTIAL** — MCP remains standalone `skipSingleton`; Pi now denies legacy execution by default but has no ControlClient/daemon semantics |
| D19 | Grant refs revalidation | grants.ts | p8 | **PASS** (local) |
| D20 | Dual-write stop self; no kill foreign 0.2 | legacy-conflict | p9 | **PASS** (local) |
| D21 | Public 0.2.4 surface at GA | goldens + MCP/Pi routes | host-entry; public surface; MCP/Pi default-denial adversarial tests | **PARTIAL** — default MCP background and Pi execution now deny rather than silently falling through, but neither supplies the one-ledger daemon lifecycle; explicit opt-outs remain 0.2 `.pi` routes |
| D22 | One journal per project domain | project-store | store-recovery | **PASS** (local) |
| D23 | Orthogonal enforcement | policy/grants | p8 | **PARTIAL** |
| D24 | CommandRecord same batch as events | AdmissionIntent claim/prepare/rebound/finalize batches | `closed-loop` P16 r1→r2→r3 journal chain; Round 56/57/75 source/audit repro | **FAIL** — queued, initial prepare, and each local rebound publish their command/run/events atomically, and rebound generation is journal-validated. The project batch still records `RunAdmitted` before coordinator commit; coordinator binding evidence remains caller-shaped and unverified (Round 75 derives only the reservation epoch); and finalize/first-dispatch crash windows lack a capability-bound recoverable transaction. |
| D25 | boundFragmentHash + executionSemanticHash | bound-fragment; hash | bound-fragment tests | **PASS** (local) |
| D26 | ArtifactRef digests; SecretRef no content | artifact-uri | p9 | **PARTIAL** |
| D27 | No DomainTransfer | surface | inventory | **PASS** |
| D28 | Node≥22.19; pnpm 11; toolchain | package engines | env.txt | **PASS** (local env) |
| D29 | ControlRegistry multi-project | registry.ts | p1 registry lock | **PASS** (local) |
| D30 | maxActiveRuns slots≡1 capacity | coordinator + process-held singleton capability + partial rebound admission saga | `closed-loop`, `store-recovery`, `stale-lock`; Round 56/57/64 authority audit | **FAIL** — main-worktree global mutations now require the process-held capability, derive reservation epochs, and reject canonical-global `baseDir` aliasing; only project-local or explicitly named non-GA fixtures persist epoch `0`. State/anchor partial-loss checks and the admission-bound unbound-release guard remain local hardening. Project admission/coordinator commitment is still non-atomic and unverified across crash, first dispatch lacks a recovery owner, and release/backpressure/pressure proof remains open. |
| D31 | RunStatus vs RunStage; parked | types.ts | wire-types | **PASS** |
| D32 | Embedded multi-mount = same singleton as taskflowd | singleton + daemon | daemon concurrent | **PARTIAL** — MCP not on multi-mount writer |
| D33 | unknown non-terminal; reconcile budget | reconcile | closed-loop | **PASS** (local) |
| D34 | Approval durability modes | types + approval.ts | wire-types; approval-cas; approval-continuation; Round 57/58 approval TTL review | **FAIL** — native approve has a narrow journaled script-continuation proof, including restart and capacity retry. Round 58 removes the forced-expiry raw exception/false-success disclosure by returning a structured, no-downstream-dispatch stop. It still has a distinct pre-commit admission path without `admissionId`, rebind/resume owner, or versioned cross-store proof; legacy approve/edit deny safely, while native edit/reject/expire/cancel semantics remain unclosed. |
| D35 | No federated multi-ControlStore | surface | inventory | **PASS** |
| D36 | reserved TTL; committed not TTL-only | coordinator | closed-loop | **PASS** (local) |
| D37 | normalRelease / forceRelease predicates | coordinator + strict admission-bound unbound-release guard | closed-loop forceRelease + Round 57 admission-release guard; force-release-approval | **FAIL** — an admission reservation is now excluded from the unbound-release path, and local force commands are audited, but `normalRelease` trusts caller booleans and force release accepts an unauthenticated string principal. Neither is bound to a terminal/parked project event and containment-backed provider proof. |
| D38 | park → release slot; approve → queued+re-reserve | native continuation + legacy denial | approval-cas; approval-continuation; Round 57/58 approval TTL review | **FAIL** — the native script happy path reaches queued/re-reserve/provider resume under restart/CAS proof. Round 58 changes the forced-TTL path from raw/false success to a structured `operator` stop before downstream dispatch, but the approval-specific reserve→project-admitted→coordinator-commit ordering still lacks admission binding, a unique recovery owner, and a release proof. It must route through the versioned saga or fail close before admission; all other decision modes and host/provider parity remain unproven. |

---

## P1–P16 (complete)

| ID | Topic | Impl | Behavior test / proof | Status |
|----|-------|------|----------------------|--------|
| P1 | Policy compiler | policy.ts | p1-semantics | **PASS** (local) |
| P2 | Empty policy host-default exposure | policy.ts | p1-semantics | **PASS** (local) |
| P3 | Domain/registry identity clone/worktree | identity.ts | p1-semantics identity | **PASS** (local) |
| P4 | Negotiation / TF_* errors | types TF_ERROR_CODES; UDS hello | uds-hardening; closed-loop | **PARTIAL** |
| P5 | Public goldens matrix | core goldens + D21 | public-surface / host-entry | **PARTIAL** |
| P6 | BoundPlan hash covers grants/policy/timeout | hash.ts linker | p1-semantics hash | **PASS** (local) |
| P7 | BoundFragment dual hash | bound-fragment.ts | bound-fragment tests | **PASS** (local) |
| P8 | Grants / enforcement | grants.ts | p8 tests | **PASS** (local) |
| P9 | Legacy conflict + artifact URI | legacy-conflict; artifact-uri | p9 tests | **PASS** (local) |
| P10 | Rollback tiers | compaction/rollback | p10 | **PASS** (local) |
| P11 | Compaction cursor / TF_CURSOR_EXPIRED | compaction; ProjectControlStore | p11 | **PARTIAL** — Round 40 moves the logical cursor floor into a validated, hash-linked `CompactionCheckpoint` journal event under the commit lock; deleting/resetting a legacy loose `compaction.json` cannot revive an expired cursor, and raw public commits cannot checkpoint beyond the durable tail. Physical segment deletion remains intentionally disabled: no retained-prefix/hash-chain handoff, Receipt reachability, crash/power-loss, trusted-root, or GA retention protocol exists. |
| P12 | Command batch re-auth | CommandRecord | partial | **PARTIAL** |
| P13 | Bootstrap / singleton / platforms | hard-link owner publish; durable epochs; process-held writer capability for release/fence/global coordinator mutation; UDS/host/store/coordinator sync fences; scheduler submit/cancel fences and narrow provider lookup | stale-lock; modes-and-stress; daemon; closed-loop read-only attach; phase-scheduler adversarial; approval-continuation; recovery-owner; control-client/CLI attach identity; script-provider cancel; cancel-authority-loss | **FAIL** — attach is read-only and refuses repair-needed state. Round 64 moves singleton release and global coordinator mutation onto an opaque process-held capability backed by private `WeakMap` membership; a copied durable lock record, caller-created object, raw global store, or canonical-`baseDir` alias cannot release the owner, initialize global capacity, or choose epoch `0`. The live capability is rechecked under the singleton transition lock and derives the durable reservation epoch; standalone and explicitly named non-GA fixtures remain the only epoch-`0` routes. Existing submit/cancel fence and exact Script lookup tests still preserve unknown/reconcile rather than false terminal/Receipt after authority loss. This removes the current Round-80 bypass but does not close P13: dead-looking owners remain intentionally unreclaimed; PID/UDS epoch agreement is not holder authentication; generic/remote providers need not implement the optional fences or exact lookup; there is no production cancel-reconcile owner or full continuation recovery route; process-tree containment, no-follow publication races, 32×100 stale/restart proof, Windows parity, and authenticated UDS principal/holder proof remain absent. |
| P14 | ControlStore engine durability | strict JSON + semantic schema + anchor-linked hash chain + same-lock recovery + parent fsync | store-recovery; closed-loop read-only derived-state; whole-root negative-capability counterexample; paths-hardening; P14 frozen scope decision 2026-07-27 | **FAIL** — anchored segments, command claims, semantic cross-domain/stream/Receipt checks, and writer-only derived recovery now fail closed. Fresh stores publish a project-root identity anchor, so deletion of only `.taskflow/` is detected; an existing header whose root anchor has been removed fails closed for mutable, read-only, `rebind`, and `new-identity` opens rather than becoming an implicit migration. Whole-project copies can mint a new identity only by explicit policy while their existing anchor is intact. The root anchor itself and pre-existing `.taskflow`, header, journal, and derived-projection symlinks are covered on Unix. Round 39 permanently proves that restoring an earlier internally consistent `.taskflow/` **and** its matching project-root anchor reopens and omits a later event. The 2026-07-27 product decision now explicitly excludes only that coherent rollback of all locally consulted authority bytes from the 0.3 GA contract; 0.3 makes no anti-rollback claim and requires no external freshness witness. The counterexample remains mandatory negative evidence and this exclusion must appear in release/operator documentation. It does not exclude partial root/anchor/segment loss, corruption, half-commit, sequence rollback, concurrent recovery, stale authority, or in-scope path replacement. Round 53 blocks a pre-created temporary-path symlink with UUID + exclusive `wx`, but a permanent post-open source-entry replacement counterexample still publishes a symlink; parent/destination replacement remains the same no-follow race class. Explicit legacy migration, durable compaction/retained-prefix proof, crash/power-loss coverage, provider/approval/other-entrypoint filesystem coverage, and complete derived-projection proof remain unclosed. Therefore P14 stays FAIL; the scope decision removes one impossible freshness requirement but promotes no row. |
| P15 | Approval protocol | approval + host + phase scheduler | approval-cas; approval-continuation; script-provider; Round 57/58 real-Script TTL review | **FAIL** — native script approve has narrow restart/CAS/terminal-path proof, but it bypasses the generic admission saga: it reserves without `admissionId` and writes project admission before coordinator commit. Round 58 ensures one forced lease-expiry window returns structured `TF_RECONCILE_REQUIRED/operator/none` rather than a raw error or `ok:true`, with no downstream marker. It does not recover/rebind that admitted continuation, create a unique first-dispatch owner, cover every crash point, or close edit/reject/expire/cancel, non-script providers, MCP/Pi parity, or all-12-phase semantics. |
| P16 | Coordinator capacity/D37 | coordinator + process-held singleton mutation capability + P16-1 queued/rebound admission slice | `closed-loop` real-Script r1→r2→r3 restart; `store-recovery` semantic/input/rollback/root tests; `stale-lock` raw-global/capability/alias tests; Round 56–58/64 reviews; force-release approval | **FAIL** — Round 64 removes two current public authority bypasses: normal user-global mutations now require the process-held singleton capability (or an explicitly named non-GA fixture mode), and reservation epochs are derived under that capability rather than accepted from request metadata. A `baseDir` spelling equal to the canonical global coordinator path is rejected, so it cannot select the standalone epoch-`0` branch. Tests prove raw global reserve creates neither state nor anchor, forged capability objects fail, a real capability persists its exact fencing epoch, and project-local/explicit non-GA fixtures alone use epoch `0`. This is a real main-worktree correction, but not capacity closure. Native approval still bypasses the generic admission binding; `RunAdmitted` precedes coordinator commit; coordinator project binding remains caller-supplied; `SlotCommitted → first dispatch` has no restart owner; release predicates remain caller-shaped; schema/migration/C3 commitment, 32×100 pressure/backpressure, and all-entrypoint lifecycle proof remain absent. Coherent rollback of all local authority bytes is separately excluded by the frozen P14 2026-07-27 scope decision and is no longer a P16 blocker; partial corruption/root loss and one-sided crash windows remain in scope. |

---

> **Effective status index (authoritative where a historic narrative row is stale):**
>
> | Item | Effective status | Reason |
> |---|---|---|
> | P13 | **FAIL** | The lock-release replacement-owner counterexample is permanent; no OS-backed compare-and-delete/holder-identity replacement protocol exists. |
> | P14 | **FAIL** | Directory fsync fails closed; coherent rollback of all local authority bytes is explicitly out of 0.3 scope, while post-open pathname replacement, compaction, and complete in-scope crash/filesystem proof remain open. |
> | P15 | **FAIL** | Round 58 removes one forced-TTL raw/false-success disclosure, but native approval still bypasses the versioned saga and has no recoverable first-dispatch owner or lifecycle closure. |
> | P16 | **FAIL** | Raw global/caller-epoch bypasses are closed locally, but no cross-store capability-bound commitment, recoverable first-dispatch saga, release authority, or pressure proof exists. |

> **Round 54 current-status correction:** P13 must be treated as **FAIL**, not merely a progress-bearing PARTIAL. `paths-hardening.test.ts` now permanently demonstrates that `withExclusiveLockFile()` can read its owner token, then have the lock directory replaced before pathname `unlink`/`rmdir`; the old releaser removes the replacement owner and lock. A token reread or `lstat` only shifts the check-to-use interval. Until an OS-backed compare-and-delete/holder-identity protocol is selected and proven, normal release has no GA-safe replacement guarantee. Round 54 also hardens a smaller honest boundary: write, fsync, close, rename, owner-candidate, and directory-sync failures preserve their causal error as `TF_DURABILITY_FAILED`, and UDS returns that code rather than flattening it to `TF_COMMAND_FAILED`. This local classification does not repair the replacement counterexample, stale-writer recovery, provider fencing, authenticated peer identity, or any P13 platform/lifecycle requirement.

> **Round 54 P14 correction (historical trust requirement superseded 2026-07-27):** the earlier statement that `fsyncDirectory()` is best-effort is historical. It now fails closed on directory open/fsync/close errors, including after a visible rename, with a typed causal `TF_DURABILITY_FAILED`; focused faults cover all three cases. P14 remains **FAIL** for the permanent post-open temp-source replacement, parent/destination races, and absent crash/power-loss/cross-platform/complete-path inventory proof. Its former non-rollbackable-root requirement is no longer part of 0.3; the coherent whole-local rollback counterexample remains mandatory negative/disclosure evidence.

> **Round 55 P16/D16/D24/D30/D37 correction (supersedes any older PASS/PARTIAL wording above; external-authority clause superseded 2026-07-27):** P16 is **FAIL**, not a progress-bearing PARTIAL. The coordinator now rejects malformed runtime input before write, validates the candidate before publishing, bounds unbound TTL, persists an elapsed reservation as `expired` before refusing commit, removes `cmd-*.json` sidecars, and makes its local command ledger unique/contiguous/idempotent/principal-bound. Those fixes do not close the proven lower bounds: deleting the shared parent control root creates new empty capacity; a raw coordinator call can submit an old epoch; `normalRelease` still trusts caller booleans and force release only a string principal; and coordinator state/command history is unbounded under a globally serialized/fsyncing path. The new permanent capacity-full test proves `admitAndRun` writes an accepted project command/runId **before** reserve; a full slot returns `TF_CAPACITY_EXCEEDED` while no Run is published, so the advertised same-command retry has no safe recovery. Therefore 23.6, 23.7, 23.12, D16, D24, D30, D37 and P16 are FAIL until a capability-bound cross-store admission/release saga, retention/backpressure protocol, and real restart/pressure evidence exist. The former external freshness/repair prerequisite is superseded; coherent whole-local rollback is excluded, while partial/root loss remains in scope. P13 remains **FAIL** per Round 54; P14 remains **FAIL** with `fsyncDirectory()` now correctly classified as fail-closed rather than best-effort.

> **Round 55 local-ledger addendum:** a persisted `requestHash` is now recomputed from the recorded set-capacity or force-release payload; the final `maxActiveRuns` must match its latest set-capacity command; a v2 ledger with no such command must retain the default genesis capacity; and a v1 custom-capacity migration materializes a synthetic, hash-bound set-capacity command before v2 publication. These are fail-closed local consistency checks only. They do not make the same-root anchor non-rollbackable, authenticate a writer, or close the project/coordinator transaction.

> **Round 56 P16-1 correction (supersedes the old capacity-full ghost-command wording, not the FAIL; external-authority clause superseded 2026-07-27):** The old capacity-full happy-path witness is locally reversed. `admitAndRun` now persists a `queued` `AdmissionIntent`, with stable command/run/continuation/admission identities; after capacity is manually freed, a fresh standalone host retried with the same command reaches exactly one Run, one matching coordinator reservation, one `DispatchIntentRecorded`, and an admission-scoped provider idempotency key. This is a **narrow local happy path**, not an admission-saga closure. Three independent reviews and temporary-repo reproductions found: (1) if r1 expires between reserve and coordinator commit, project `prepareAdmission` accepts the stale r1 snapshot, coordinator marks r1 expired, a retry creates r2, and project then rejects r2 because its durable Run names r1; (2) after `SlotCommitted` before first intent, retry returns `TF_RECONCILE_REQUIRED` while `reconcilePendingDispatch` cannot act without an active attempt; (3) `RunAdmitted` precedes coordinator commit and lacks authoritative admission linkage. A direct unbound release can create the same wedge. The variants retain schema 1 without migration/downgrade. Therefore the listed rows remain **FAIL** until a capability-bound, versioned, crash-tested saga, release proof, and bounded pressure evidence exist. The former external freshness/repair prerequisite is removed only for coherent whole-local rollback.

> **Round 57 P16 expiry/release correction (supersedes only Round 56 A1/A2 wording, not the FAIL):** The generic pre-provider r1/r2 wedge is now a permanent real-`ScriptExecutionProvider` fault-injection test. Two separate fresh hosts force r1 then r2 to expire after `reserve()` returns and before coordinator commit; each leaves a durable `project-prepared` Run, returns structured `TF_RECONCILE_REQUIRED` with `retry-same-command` and `sideEffects:none`, writes no marker, and a third fresh host records an immutable `AdmissionReservationRebound` generation before one r3 dispatch/completed marker. The marker is append-counted, so two completed Script executions would fail the exact `once\n` assertion. `releaseUnboundReservation()` now rejects any reservation carrying `admissionId`. This is deliberately a local, `skipSingleton + standalone` generic rebind protocol: it does not prove the replacement reservation is capability-held/live at project prepare, does not make the pre-commit `RunAdmitted` evidence authoritative, and does not close A3/A4, migration, external freshness, retention, pressure, or entrypoint parity. Independent review additionally reproduced native approval's distinct reserve-without-admissionId false-success wedge, a stale r2→r3 raw-error path, and a no-dispatch pre-provider capacity-full misclassification; therefore P15 is also **FAIL**. All listed rows remain **FAIL**.

> **Round 58 first-dispatch truthfulness correction (supersedes only the A4/native false-success wording, not any FAIL):** two permanent real-writer crash fixtures now kill the process (a) after durable `RunStatusChanged(executing)` but before `AttemptPrepared` / `DispatchIntentRecorded`, and (b) after durable `SlotCommitted/running+admitted` but before the first intent. In (a), a same-command retry now returns `TF_RECONCILE_REQUIRED / operator / unknown`, writes no Script marker, and issues no Receipt; it no longer falsely reports `ok:true`. A follow-up adversarial fixture also stores a schema-valid `activeAttempt:prepared` snapshot after that crash: `prepared` is not accepted as proof of a first intent, while `intent-recorded` / `acknowledged` / `ambiguous` remain the local disclosure boundary. In (b), the intentionally green counterexample still returns the same structured stop with zero intents/markers, proving there is still no restart owner. A native approval forced-TTL fixture now proves its initial call and same-command retry return `TF_RECONCILE_REQUIRED / operator / none`, not a raw coordinator exception or `ok:true`, and make no downstream intent/marker. These are safety classifications only: no admission ID was added to native approval, no reservation is rebound, no owner may create the missing first intent, and the project/coordinator ordering, migration, entrypoint, pressure, trust-root, and release blockers remain. P15, P16, §23.6/§23.7/§23.11/§23.12, D16/D24/D30/D34/D37/D38 remain **FAIL**.

> **P13 Round-33 correction (supersedes the stale-takeover wording in historical rows):** a dead-looking singleton owner is no longer read-then-unlinked or replaced. Normal auto bootstrap returns `TF_DURABILITY_FAILED` and leaves the owner pathname and epoch sidecar unchanged. The cross-process Script fixtures now first prove that boundary; their later `lookupByIdempotency` / `reconcilePendingDispatch` assertions use explicit `skipSingleton` test plumbing only, so they exercise a narrow recovery state machine rather than a production new-epoch handover. This removes the unsafe stale-steal claim, but leaves automatic liveness unavailable. Round 34 closes only one attach-spoof subcase: `SingletonLockInfo.endpoint` must be the canonical UDS path, and CLI/control-client require an exact protocol/role/epoch hello for the probe and the later RPC connection. That detects a live unrelated PID plus a mismatched forged UDS; it does **not** authenticate a holder or prove safety against a forged endpoint carrying the same publicly stored epoch. No authorized operator/OS-backed replacement protocol, full public recovery lifecycle, generic-provider fence/cancel proof, 32×100 stale/restart proof, no-follow race proof, process-tree cancellation proof, or Windows proof exists.

> **P13 Round-35 cancellation correction:** the Round-34 stale-CAS counterexample is now a real Script-provider red/green test: a stale `expectedRunVersion` returns `TF_STALE_VERSION` before any provider signal. A matching command first commits `cancelRequest.state=requested`, then `signalling`, before the provider is called; a same-command retry after any possible signal returns reconciliation rather than signalling again. The initial process-group implementation was itself disproved by a detached-grandchild fixture: it returned `cancelled` while a descendant outside the group later wrote its marker. The current Script cancel **and reconcile** paths therefore return `ambiguous`, retain the reservation and expose `unknown/reconciling`; ordinary inherited-group and deliberately detached-descendant tests both assert that nonterminal result. This is an honesty/safety reduction, not a cancellation protocol closure: raw `child_process` has no whole-tree containment, there is no cancel recovery owner, generic providers are not required to honor the fence or prove quiescence, public UDS/CLI/MCP/Pi parity is absent, and Windows remains non-GA.

> **P13 Round-36 C3 crash correction:** a real helper process now stops at the actual negative process-group `SIGKILL` call only after durable Script `cancelRequestedAt` exists; the parent kills that helper before the OS signal. A fresh, explicit `skipSingleton` test actor retries the same command and proves zero second provider call, `TF_RECONCILE_REQUIRED` / `sideEffects:possible`, durable `signalling`, `unknown/reconciling`, retained reservation, no Receipt, and a still-live Script. It proves a safe stop for this exact unknown-side-effect window, not a production restart authority, a signal observation, tree quiescence, or generic-provider capability. C1 same-command multi-process convergence remains the next mandatory test.

> **P13 Round-37 C1 command-race correction:** two real Script subprocesses are forced to read the same absent cancel command, then the owner alone journals `requested → signalling` and parks at the actual group signal boundary before the loser attempts its stale CAS. The old path returned `TF_STALE_VERSION`; the current path rereads only an exact same-principal/same-request/same-run command and returns `TF_RECONCILE_REQUIRED` / `sideEffects:possible` / `reconcile`. The permanent fixture proves one actual group signal, one durable target command, final `unknown/reconciling`, retained reservation, Receipt absence, and a cross-principal retry with no Run/snapshot or second signal. `skipSingleton` is test plumbing, so this is not a production multi-writer/public-entrypoint/stress proof. C2 pre-`signalling` crash remains mandatory.

> **P13 Round-38 C2 requested-before-signalling crash correction:** a real subprocess now parks immediately after the actual ControlStore `CancelRequested` commit, before the separate `signalling` CAS and before `ScriptExecutionProvider.cancel()` can durably record a signal intent or call the OS. The parent verifies the direct journal segment contains exactly one target command and `cancelRequest.state=requested`, the durable Script job is still `running` with no `cancelRequestedAt`, and the Script is live; it then `SIGKILL`s the original caller. A fresh explicit `skipSingleton` test actor retries the exact same command and proves `TF_RECONCILE_REQUIRED` / `sideEffects:unknown` / `reconcile`, zero provider cancel calls, unchanged durable `requested`, retained reservation, no Receipt, no durable signal intent, and a still-live Script. This is a narrow fail-closed proof of the no-guess rule for one Script crash window, not a production replacement authority, liveness protocol, containment observation, generic-provider contract, public entrypoint lifecycle, or Windows proof.

> **P13 Round-52 C7-B admission/observation correction (supersedes the Round-51 direct-foreign-terminal wording):** before acknowledging a newly returned handle and once immediately before entering terminal observation, the scheduler synchronously calls `providerHandleOwnershipFailure()` against the current `(providerName, handle, runId:phase)` record. A provider with no `loadHandle`, no durable record, or a same-name record for another Run is not trusted: a newly returned handle remains outside `DispatchAcknowledged`; a historical acknowledgement remains historical evidence only and is not observed. `ControlHost` makes the outcome `unknown/reconciling + needsOperator`, retains the reservation, issues no Receipt, and returns `TF_RECONCILE_REQUIRED`. Permanent local red/green tests cover a foreign accepted handle, a foreign historical acknowledgement, and missing lookup; they assert no `poll`/`collect` where applicable. A permanent Round-52 C7-C counterexample then returns matching records for both checks, rebinds the opaque handle immediately afterwards, and observes a foreign completion/Receipt/slot release. This is therefore a narrow local C7-B static-ownership safety PASS, **not** C7/P13/D38 closure: the provider record can change after the check, there is no stable provider incarnation or remote/shared linearization, generic providers may not implement a trustworthy durable ledger, restart/late-ack/migration/public lifecycle remain unproven, and P14 remains independently FAIL.

## Required adversarial tests (objective)

| Case | Test location | Status |
|------|---------------|--------|
| mixed agent+script both execute | `phase-scheduler-adversarial.test.ts` | **PASS** (local) |
| two dependent scripts; first marker | same | **PASS** (local) |
| upstream fail → no success Receipt | same | **PASS** (local) |
| MCP identity/store mismatch no marker | p9 legacy / identity | **PARTIAL** — TF_LEGACY_CONFLICT / identity; not full MCP mismatch e2e |
| daemon writer + CLI auto via UDS | `uds-hardening.test.ts` | **PASS** (local) |
| CLI attach rejects lock/UDS epoch mismatch | `cli.test.ts`; `control-client.test.ts` | **PASS** (local) — canonical endpoint plus probe/RPC hello pinning; not holder authentication |
| RPC before hello no side effect | `uds-hardening.test.ts` | **PASS** (local) |
| missing/unknown projectId denied | `uds-hardening.test.ts` | **PASS** (local) |
| MCP run/status/wait/cancel/approval same ledger | default denial regression + legacy opt-out suite | **FAIL** — default MCP background/run-status/resume now deny before `.pi`; the explicit opt-out remains 0.2 and there is still no common ledger, approval, or daemon lifecycle |
| 32-process stale takeover ×100 = one writer | one 24-process fresh contention test | **FAIL** — the fresh contention proof is stronger (writer held through every peer observation), but it is not stale takeover. Crashed authority recovery is deliberately unavailable, so no 32×100 recovery/liveness evidence exists. |
| old epoch writer mutation rejected | `stale-lock`, daemon fencing, `phase-scheduler-adversarial`, `approval-continuation`, daemon late-fence UDS test | **PARTIAL** — focused synchronous checks pass. Real-script tests deny ordinary/native-approval submission after durable intent when the next authority check fails, and separately simulate loss immediately after that host check: ScriptExecutionProvider reaches its synchronous fence before idempotency reserve/spawn, emits no marker/Receipt, and retains the intent/reservation. A second ordinary/native test simulates loss after provider spawn: the marker may exist, but the old epoch returns structured `TF_AUTHORITY_REVOKED` + `sideEffects:possible` + `reconcile`, keeps the durable checkpoint `intent-recorded`, and never signs a Receipt. A direct UDS test preserves a late `SingletonAuthorityError` code. This is not a generic provider or full recovery proof: remote providers may ignore the fence; no new writer yet owns unacknowledged-handle reconciliation; full RPC side-effect metadata, stale takeover, and attach/open/recovery/registry/cancel interleavings remain open. |
| stale-CAS cancel → zero provider signal | `script-provider.test.ts` real Script fixture | **PASS** (local narrow boundary) — an old `expectedRunVersion` returns `TF_STALE_VERSION`; wrapped provider cancel call count remains zero and the real script stays live |
| same cancel command two-process stale-read race | `script-provider.test.ts`; `helpers/mp-cancel-same-command-race.mts` | **PARTIAL** — exact real Script interleave now converges both same-principal contenders to reconcile with one OS signal and no foreign disclosure; no normal singleton/UDS/MCP/Pi parity, crash recovery, or 32×100 proof exists |
| crash after durable Script cancel intent, before OS signal | `script-provider.test.ts`; `helpers/mp-cancel-after-intent-before-signal.mts` | **PARTIAL** — real subprocess kills the caller at the actual signal boundary; a fresh test-authorized actor proves same-command zero re-signal/nonterminal/reservation retention, but no production cancel-recovery authority, generic-provider proof, or containment observation exists |
| cancel kills process tree; no grandchild marker | `script-provider.test.ts` | **PARTIAL** — ordinary inherited Unix group is killed and its marker never appears, but a detached descendant demonstrably escapes the group; raw Script cancellation must not claim whole-tree proof |
| ambiguous cancel nonterminal | `script-provider.test.ts` | **PARTIAL** — real ordinary and detached-descendant fixtures retain `unknown/reconciling`, the reservation, and one durable signal intent. There is still no restart/operator reconciliation lifecycle that can safely settle it. |
| approval cannot complete unless provider completes | approval-cas; mcp-bind; approval-continuation | **PARTIAL** — legacy unavailable continuation remains safely parked; a narrow positive native script proof covers restart/CAS and provider-terminal Receipt issuance. It does not cover every approval decision, provider, crash boundary, or public host entrypoint. |
| capacity exceeded retry-same-command | `approval-continuation.test.ts` | **PARTIAL** — native script approval proves queued same-command retry after a fresh-host restart and four concurrent retry clients with one downstream side effect; generic capacity lifecycle, other decisions/providers, and public-entrypoint parity remain open. |
| coordinator state/anchor/directory missing after reservation | `store-recovery.test.ts` | **PASS** (local) — initialized state, anchor, and coordinator-directory deletion deny reopen without recreating capacity; v1 migration is covered. |
| coordinator parent control-root deletion or rollback | — | **FAIL** — deleting or rolling back both state and its parent anchor remains indistinguishable from first use; no authorized repair/auditable history exists. |
| corrupt header/journal/seq fail-closed | store-recovery; closed-loop attach | **PARTIAL** — malformed files, prefix/all journal loss, hash-chain payload tamper, cross-domain projections, duplicate/gapped streams, and unbacked Receipt manifests fail closed; attach refuses rather than repairs stale derived state. The named P14 whole-root snapshot counterexample proves that a coherent older state is distinguishable from neither a legitimate older state nor a local-only anchor chain. Parent/root rollback, legacy migration, crash/power-loss, and hostile-filesystem race proof remain open. |
| pre-existing control-path symlink cannot redirect durable authority | store-recovery; stale-lock | **PARTIAL** — Unix red tests cover project `.taskflow`, header, journal, projection status read, user coordinator root, and singleton root; external sentinels remain unchanged. No `openat`/`O_NOFOLLOW` primitive, concurrent replacement race, provider/approval/registry exhaustive inventory, or Windows proof exists. |
| Receipt verifier tamper / missing enforcement | issueReceipt honesty | **PARTIAL** — no full tamper suite |
| packed clean consumer daemon+CLI e2e | pack smoke | **PARTIAL** — 12-pkg smoke; not full packed daemon+CLI path proven every tip |

---

## Round notes (recent)

### Round 16 — phase scheduler + host LLM (`558f28ad`)
- `phase-scheduler.ts`, `host-llm-provider.ts`; admitAndRun per-phase; MCP fg injects LLM
- Adversarial multi-script / mixed / upstream fail
- unit **2131** then advanced

### Round 17 — ControlClient + UDS harden (`94d5b7b7`)
- control-client; CLI attach UDS; hello-before-RPC; exact projectId
- unit **2134**

### Round 18 — matrix complete rewrite + SCRATCH evidence package
- Full §23 + D1–D38 + P1–P16 (this file)
- Dual-path inventory explicit FAIL/PARTIAL
- SCRATCH logs + sha256 in `ga-evidence-report.md`
- **Verdict remains NOT closed-loop GA**

### Round 19 — durability and singleton adversarial slice (uncommitted)
- strict P14 recovery: corrupt header/journal/seq/coordinator fail closed; missing sequence rebuild; recovery shares the commit lock
- P13: complete owner candidate → hard-link publish; durable non-reused epochs; stale host/UDS/direct store/coordinator writes fenced
- initial local `pnpm test`, `test:control` (93), `test:daemon` (7), `test:cli` (4), and typecheck passed; evidence remains local only
- **Verdict remains NOT closed-loop GA** — no matrix row was promoted to PASS

### Round 20 — adversarial audit recheck (uncommitted)
- a fresh `pnpm test` failed in `approval-cas`: one child exhausted the fixed retry budget for `commit.lock`; immediate focused `test:control` passed 93, so the aggregate green is timing-sensitive rather than repeatable proof
- static stale-takeover review: owner and authority stale removal are non-CAS; the hard-link publish does not prevent a previously authorized contender from unlinking a new owner
- static P14 review: no durable journal/compaction anchor; shallow journal payload validation; stale derived artifacts survive recovery; command claim is off-journal
- coordinator deletion review: an initialized `state.json` is interpreted as a fresh coordinator when missing, resetting capacity and losing occupancy
- **Verdict remains NOT closed-loop GA** — P13 and P14 are now explicit FAIL rows, and no tag/publish/GA wording is allowed

### Round 21 — P13/P16 fail-closed durability slice (uncommitted)
- generic directory locks now publish a fsynced random owner token and never reclaim a contended pathname; stale-looking locks wait and then return `TF_DURABILITY_FAILED`, rather than deleting uncertain authority
- the 24-process fresh singleton test now holds its writer until all peers have recorded their observed role; this repairs the former timing-invalid test but does **not** stand in for stale takeover ×100
- coordinator v2 pairs `state.json` with a parent `coordinator.anchor.json`; initialized state/anchor/directory loss fails closed, while v1 state migrates only as part of a mutation
- a fresh local root suite passed **2152/2152**, but the tree is dirty and uncommitted, the run is local only, and P13/P16 remain **PARTIAL** rather than GA PASS
- **Verdict remains NOT closed-loop GA** — P14 journal anchoring, approval/lifecycle truth, unified entrypoints, stale-authority recovery, and remote clean-candidate evidence are still required

### Round 22 — P14 anchored journal and derived-state recovery (uncommitted)
- every newly written journal segment now carries a SHA-256 predecessor link; a parent `journal.anchor.json` is written before the intended segment, so prefix/all deletion or payload substitution fails closed rather than opening a shorter history
- command claims now consume a journal commit instead of being written only to the derived command index; recovery rebuilds commands and removes ghost run/command/receipt indexes not backed by journal records
- opening the same-path header is read-only; a corrupt journal no longer refreshes `header.updatedAt` before failing. Physical compaction deletion is deliberately deferred until a durable checkpoint can hand off the chain
- red tests covered no anchor, prefix/all deletion, hash tamper, lost claim indexes, and ghost projections; final explicit local gates passed: root **2157/2157**, control **104/104**, daemon **7/7**, CLI **4/4**, typecheck, and `git diff --check`
- **Verdict remains NOT closed-loop GA** — P14 is still FAIL (untrusted parent/root loss, legacy migration, compaction, full schemas, and crash matrix); no tag/publish/GA wording is allowed

### Round 23 — read-only attach, semantic P14, and honest P15 denial (uncommitted)
- singleton attach opens the ControlStore and registry in a read-only mode: it waits only for monotonic writer initialization, never acquires writer/recovery locks or registers itself, and rejects stale/missing derived indexes rather than rebuilding them
- P14 now validates full event/run/command/Receipt shapes and cross-entity constraints; adversarial tests recompute hash/anchor after cross-domain projection substitution, duplicate/gapped stream edits, and Receipt manifest forgery, and all reopen attempts fail closed
- a real Receipt-manifest generation defect was corrected for normal and reconciled completion: the pending `ReceiptIssued` event id is now included in the signed manifest
- approve/edit no longer manufacture a completed Run or Receipt when no durable provider continuation exists; they return `TF_FEATURE_REQUIRED`, leave the request pending and Run parked, and MCP follows the same behavior
- fresh local control checks pass, but this is a safety reduction rather than P15 completion. It does not create durable attempts/resume cursors, a provider idempotency protocol, or the required queued→re-reserve→provider lifecycle
- **Verdict remains NOT closed-loop GA** — no tag/publish/GA wording is allowed

### Round 24 — native approval continuation and script idempotency (uncommitted)
- native `approval` now parks a journaled immutable plan/cursor and a fresh host can approve, re-reserve, and resume only the downstream script phase; the focused restart proof preserves the already completed side effect
- a four-process approval CAS race has one winner, one downstream side effect, and stale-version losers; Receipt issuance occurs only after the downstream provider reaches terminal completion
- ScriptExecutionProvider now binds a durable stable attempt key to one request shape; four provider processes converge on one handle, while a reservation lacking durable job acknowledgement becomes `ambiguous` rather than a duplicate spawn
- mutable writer journal reads now wait under `commit.lock` during anchor publication; read-only attaches remain fail-closed observers
- **Verdict remains NOT closed-loop GA** — P15 is still PARTIAL and P14 remains FAIL; all non-script decision/provider/host paths, capacity retry, crash matrix, semantic parity, packed proof, and remote CI remain required

### Round 25 — native capacity retry command ownership (uncommitted)
- an accepted native approval command that reaches `running/queued` because capacity is full now retries the same durable continuation rather than merely returning that queued snapshot
- admission binds a newly reserved slot to the still-queued continuation under the project `compareAndCommit` lock before any downstream provider dispatch; a competing retry releases its unbound reservation and only observes the owner’s Run/Receipt
- the proof is deliberately narrow but stronger: after capacity frees, the same command resumes across a fresh host without replaying upstream work; four concurrent processes retrying that already-accepted command produce one downstream side effect
- **Verdict remains NOT closed-loop GA** — this closes only the native script capacity-retry subcase. P14 remains FAIL; P13/P15/P16 and the dual-path, semantic, crash, packed, and remote-CI gates remain unresolved.

### Round 26 — unbound reservation release discipline (uncommitted)
- retry-race losers now call `releaseUnboundReservation`, which accepts only a `reserved` record with no project, run, admission, attempt, or provider binding; it is explicitly distinct from D37 release of an admitted or ambiguous Run
- the coordinator test proves that an unbound reservation can be released while a committed Run-bound reservation is rejected by that API; the native retry fixtures use this exact path rather than asserting a false parked state
- **Verdict remains NOT closed-loop GA** — this removes a misleading local predicate from one pre-admission loser path. It does not solve the cross-store project-admission/coordinator-commit crash window or any P16 root/history/repair gap.

### Round 27 — pre-existing symlink fail-closed slice (uncommitted)
- `assertNoSymbolicLinkBelow` treats the chosen project/home root as the trust boundary and rejects a pre-existing descendant symlink before ControlStore open/recovery/mutation/status reads, user coordinator state mutation, or singleton authority/epoch/UDS-path publication
- strict durable JSON reads reject a symlink leaf; ControlStore layout checks cover journal/derived directories on every public durable read and commit-lock entry, so a **pre-existing** journal/projection substitution fails before reading or writing the external target. This is not a post-check/open concurrent-replacement guarantee.
- Unix red tests keep external sentinel bytes/entries unchanged for project `.taskflow`, header, journal mutation, projection status read, user coordinator root, and singleton root
- **Verdict remains NOT closed-loop GA** — this is lstat-only pre-existing-path evidence, not an OS no-follow concurrent-replacement protocol; P13/P14/P16 stay PARTIAL/FAIL and the full entrypoint/provider/approval/compaction/rollback matrix remains open.

### Round 28 — project-root identity-anchor slice (uncommitted)
- a fresh ControlStore writes `.taskflow-control.anchor.json` outside the mutable `.taskflow/` subtree before its matching header; reopening after deletion of only `.taskflow/` fails closed without recreating a new authority
- the root anchor must match `projectId` and `controlDomainId`; a whole-project clone can replace it only through the existing explicit `identityPolicy: "new-identity"` path, and the new runtime artifact is ignored by Git
- Unix tests reject a pre-existing symlink at the root-anchor pathname without touching an external sentinel; the focused recovery/identity suite includes root-loss, mismatch, copy/new-identity, and symlink cases
- **Verdict remains NOT closed-loop GA** — this is a narrow fresh-store detection boundary. Legacy header-only stores, deletion/rollback of the project root together with its anchor, hostile concurrent replacement, repair authorization, compaction, crash/power-loss, provider/approval paths, and remote clean-candidate evidence remain open; P14 stays FAIL.

### Round 29 — P13 pre-submit authority and UDS semantic slice (uncommitted)
- `schedulePhases` now asks its host for a fresh authority decision immediately after durable `DispatchIntentRecorded` and immediately before every new `ExecutionProvider.submit`; a denial returns the active `intent-recorded` checkpoint without terminalizing the Run or releasing its capacity reservation
- real `ScriptExecutionProvider` marker tests cover both ordinary admission and a native approval continuation: the initial command/approval check is permitted, then the next pre-submit check loses authority; no external marker or Receipt appears and the durable attempt remains for recovery
- UDS preserves a late `SingletonAuthorityError` from a real ControlHost durable mutation fence as `TF_AUTHORITY_REVOKED`; this wire test covers error classification only, not a complete RPC side-effect/retry payload
- **Verdict remains NOT closed-loop GA** — the guard is a narrowing check, not a provider-held/atomic fence. A takeover immediately after it may still submit; generic providers lack a unique intent-resume protocol; stale takeover/PID identity, cancel/ack fencing, 32×100 recovery, UDS authenticated/fenced lifecycle, Windows, P14, entrypoint unity, phase semantics, approval breadth, and remote candidate evidence remain open.

### Round 30 — Script provider fenced submission slice (uncommitted)
- `ExecutionProvider.submit` now accepts an optional synchronous submission fence. ControlHost supplies one after its durable intent/host authority check; ScriptExecutionProvider executes its idempotency reservation, real OS `spawn`, and durable provider-job acknowledgement inside that same fence.
- a new real-script red/green test permits the public admit and host pre-check, then revokes the durable fence before the provider critical section. The old implementation spawned and later failed while persisting `DispatchAcknowledged`; the new path throws `TF_AUTHORITY_REVOKED` before reserve/spawn, keeps the journaled `intent-recorded` attempt and reservation, and creates neither marker nor Receipt.
- **Verdict remains NOT closed-loop GA** — this narrows one in-process Script provider handoff, not the general provider protocol. Host LLM/remote providers do not yet prove fence enforcement, a loss after Script provider returns but before the project-journal acknowledgement still needs unique recovery semantics, stale takeover itself is unsafe, and all broader P13/P14/P15/P16/entrypoint/remote gates remain open.

### Round 31 — post-spawn acknowledgement-loss containment slice (uncommitted)
- after a Script provider has durably accepted/spawned a handle, `onDispatchAcknowledged` can still lose singleton authority before the ControlStore journal records that handle. Scheduler now catches this authority-specific boundary and returns the original `intent-recorded` checkpoint rather than terminalizing or releasing capacity.
- ControlHost turns that condition into structured `TF_AUTHORITY_REVOKED`, `sideEffects:possible`, and `recoveryAction:reconcile`; real ordinary and native-approval script tests prove an external marker may exist while no Receipt is issued and the project journal intentionally has no forged provider handle.
- **Verdict remains NOT closed-loop GA** — containment is not recovery. No authoritative restart path yet resolves the provider handle from the idempotency record and proves exactly-once reconciliation; generic providers, stale takeover, full UDS error payloads, cancel lifecycle, cross-process barrier proof, and all broader gates remain open.

### Round 32 — cross-process Script intent lookup/recovery slice (uncommitted)
- `ExecutionProvider` now has an optional but explicit `lookupByIdempotency` recovery contract: a `not-found` answer must prove that the exact request owns neither reservation nor handle; a provider without it is not eligible for automatic recovery. Script lookup validates the complete request shape and trusts only the current durable job acknowledgement, never a stale in-memory job cache.
- `ControlHost.reconcilePendingDispatch` is a conservative ControlHost-only recovery endpoint. It reconstructs the active phase micro-program from the immutable BoundPlan, performs lookup without `submit`, atomically records `DispatchAcknowledged` only for the returned exact handle, calls `reconcile` for observation, and intentionally leaves the Run `unknown/reconciling` + `needsOperator` with no Receipt instead of silently continuing the DAG.
- real cross-process auto-singleton fixtures SIGKILL the old writer after its host pre-check/before Script fence and after real Script spawn/before project acknowledgement. The first path has no marker/handle and recovery does not replay; the second path has one marker and the new epoch looks up/reconciles the same handle with zero replacement submits. A contract test proves an opaque provider with no lookup is `unsupported` and gets no automatic submit.
- **Verdict remains NOT closed-loop GA** — this is a narrow Script crash-window owner, not a full restart protocol. It is not routed through daemon/UDS/CLI/MCP/Pi, cannot resume/terminalize all phase semantics or issue a Receipt, and does not solve generic provider fencing, singleton PID/path takeover, callback/cancel fencing, P16 cross-store recovery, 32×100 stress, P14, or remote clean-candidate evidence.

### Round 33 — stale singleton fail-closed correction (uncommitted)
- A dead-looking singleton owner is no longer unlinked or replaced from a PID check. Auto bootstrap returns `TF_DURABILITY_FAILED` and leaves the owner pathname and epoch sidecar unchanged; the focused stale-lock red/green test proves that boundary.
- The former Round-32 crash fixtures were reclassified: after SIGKILL they first assert that a normal auto host cannot claim a new epoch. Their subsequent `lookupByIdempotency` / `reconcilePendingDispatch` checks run only through explicit `skipSingleton` test plumbing, so they validate the narrow Script recovery state machine but are not evidence of a production stale-writer handover.
- The first Round-33 root run correctly failed **2/2188** because those tests still asserted automatic takeover; the raw failed log is retained. After the contract correction, the final local matrix passed: root **2188/2188**, control **134/134**, daemon **8/8**, CLI **4/4**, typecheck, and `git diff --check`.
- **Verdict remains NOT closed-loop GA** — safety improved by refusing unprovable recovery, not by solving liveness. Holder-authenticated/OS-backed recovery, a public recovery lifecycle, generic provider fencing/cancel, P14/P15/P16, single-entrypoint semantics, packed candidate proof, and remote CI remain blockers.

### Round 34 — CLI attach endpoint identity binding (uncommitted)
- A red CLI fixture created a live unrelated PID, a forged UDS at the canonical pathname, and a singleton record whose epoch differed from the forged `hello-ok`. Before the change, `status` returned the forged RPC result; the final path returns `TF_BOOTSTRAP_FAILED` with `via: "uds-identity-mismatch"` and sends no RPC.
- `singleton.ts` now rejects a lock record whose endpoint differs from the current canonical UDS pathname. `control-client.ts` validates hello shape/protocol and accepts caller-supplied expected role/epoch; `cli.ts` pins both the initial probe and each new RPC connection to the singleton record's writer epoch.
- This closes endpoint-swap and mismatched-epoch confusion for the CLI attach path only. It cannot prove that a responder claiming the same public epoch is the lock holder, does not authorize stale replacement, and does not make cancel/provider callbacks fenced.
- Fresh local gates: root **2191/2191**, control **136/136**, daemon **8/8**, CLI **5/5**, typecheck, and `git diff --check` passed. Evidence remains a dirty local worktree only.
- **Verdict remains NOT closed-loop GA** — no P13/P14/P15/P16 or entrypoint row was promoted to PASS.

### Round 35 — stale-CAS cancel and process-group counterexample correction (uncommitted)
- A real Script provider stale-version red test now proves `ControlHost.cancel()` performs its CAS-backed durable cancel intent before touching the provider: stale `expectedRunVersion` returns `TF_STALE_VERSION`, provider cancel call count stays zero, and the real job remains live.
- The first attempt to use a detached Unix process group as terminal proof was falsified by a fixture whose child spawns a detached grandchild. Before the correction the host returned `cancelled`, then the escaped grandchild wrote a marker. The raw negative record is retained in SCRATCH. Script cancellation now journals `requested → signalling`, emits at most one signal for a command, and both Script cancel/reconcile return `ambiguous` / `unknown/reconciling` even after the ordinary group exits; neither the slot nor a terminal status is released/issued from that observation.
- Fresh permanent fixtures cover both an inherited child (its marker never appears) and a deliberately detached child (its marker does appear) while asserting the same conservative nonterminal result. They are Unix-only; Windows is explicitly not covered.
- **Verdict remains NOT closed-loop GA** — this is a removal of a false terminal claim. A containment-backed process-tree primitive, durable cancel recovery/repair, generic provider capability proof, public-entrypoint routing, P14/P15/P16 closure, packed candidate, and remote CI remain required.

### Round 36 — durable cancel intent / pre-signal crash boundary (uncommitted)
- A real subprocess enters `ScriptExecutionProvider.cancel`, lets it durably write `cancelRequestedAt`, and is then deliberately killed at the actual negative process-group `SIGKILL` boundary before the OS receives the signal. The parent checks the durable provider job and live Script before killing the helper, so this is neither a mock nor an after-the-fact status inference.
- A separately created, explicit `skipSingleton` test actor retries the exact same command. It makes zero provider cancel calls, reports `TF_RECONCILE_REQUIRED` with `sideEffects:possible`, preserves durable `signalling`, `unknown/reconciling`, the reservation, and Receipt absence, and leaves the Script alive. The test actor is not evidence that normal auto bootstrap may replace a crashed singleton owner.
- **Verdict remains NOT closed-loop GA** — C3 closes only a narrow fail-closed crash safety cell. C1 concurrent same-command convergence, C2 pre-signalling crash, cancel recovery/repair authority, containment-backed terminal observation, generic providers, public entrypoints, P14/P15/P16, a clean packed candidate, and remote CI remain required.

### Round 37 — same-command cancellation race correction (uncommitted)
- Two real helper processes are made to read an absent cancel command before either commits it. The owner then records `requested → signalling` and parks immediately before the actual Script group signal; only after that does the stale reader attempt its old CAS. The red path returned `TF_STALE_VERSION` even though it had the same command/principal/request and the signal owner was already durable.
- `ControlHost.cancel()` now rereads the command after a failed initial cancel CAS. Only an exact same `(requestHash, kind=cancel, callerPrincipal, runId)` command reaches the existing-command resolver; it returns reconcile rather than re-signalling. An existing `requested` command is likewise non-takeover: only the original call stack can advance it, so a later retry cannot invent a second signal owner.
- The permanent test proves both contenders expose the same reconcile contract, exactly one real OS group signal, one command record, retained nonterminal capacity/no Receipt, and foreign-principal non-disclosure. The test is explicit `skipSingleton` storage plumbing, not a public multi-host or crash-recovery proof.
- **Verdict remains NOT closed-loop GA** — C1 is a narrow local safety cell. Its formerly-next C2 pre-signalling crash is now covered by Round 38, but C4 authority loss, C7 provider capability, C8 public lifecycle, C9 stress, containment-backed terminal proof, P14/P15/P16, packed candidate, and remote CI remain required.

### Round 38 — `requested` before `signalling` crash safety (uncommitted)
- A real helper process calls the actual `ControlHost.cancel()` path and monkeypatches only its local test Store handle so it parks immediately after the durable `requested` commit. It has not executed the subsequent `signalling` CAS or entered the provider cancel method. The parent validates the raw journal bytes and the durable provider job before killing that caller.
- A fresh explicit `skipSingleton` actor reopens the same project with the same command. It cannot infer that it owns the original signal: the result is `TF_RECONCILE_REQUIRED`, `sideEffects:unknown`, and `reconcile`; wrapped provider cancel count remains zero. The Run stays `unknown/reconciling + requested`, capacity remains reserved, Receipt remains absent, and the never-signalled Script remains live.
- Fresh local gates: focused C2 **1/1**, full Script-provider file **14/14**, root **2197/2197**, control **142/142**, daemon **8/8**, CLI **5/5**, typecheck, and `git diff --check` all passed. This binds only the current dirty local worktree through the Round-38 manifest; no remote action occurred.
- **Verdict remains NOT closed-loop GA** — C2 is a narrow local crash-safety cell. It does not create a legitimate recovery owner or a signal/terminal observation. Overall priority remains P14 trusted-root/rollback/compaction; the next cancellation-specific red cell is C4 authority loss, followed by containment and adversarial-provider capability, not a `cancelled` status promotion.

### Round 39 — P14 whole-root rollback counterexample and trust-decision boundary (uncommitted)
- The permanent `store-recovery` adversarial fixture commits event A, copies the exact current `.taskflow/` subtree and `.taskflow-control.anchor.json`, then commits event B. It restores the old subtree and matching anchor at the same project path. `openProjectControlStore()` succeeds, retains the same project/domain identity, and exposes only A. Therefore the existing files-only anchor/hash chain accepts a coherent earlier history rather than detecting rollback.
- This is a lower-bound proof, not a request to add another local digest: when every input the opener reads can be replaced by bytes from a legitimate old state, a local-only algorithm observes the same bytes in the legitimate-old and rolled-back worlds. P14 cannot claim freshness until product chooses an external monotonic witness, an authenticated explicit repair/rebind protocol that fails closed on ambiguity, or a narrowed non-GA deployment claim.
- The P14 ADR now records that decision boundary and its minimum design obligations. No source path is promoted to GA, no automatic repair is added, and no local snapshot rollback is described as safe.
- **Verdict remains NOT closed-loop GA** — P14 stays FAIL. The exact next P14 artifact is an approved trust/repair choice and threat model; only after it exists may implementation add a witness or repair protocol and inject crash/reopen faults across its writes. Existing compaction, no-follow race, power-loss, legacy, entrypoint, P13/P15/P16, clean candidate, and remote-CI gates remain open.

### Round 40 — P11 journal-bound cursor checkpoint (uncommitted)

- The old independent `compaction.json` was a real resurrection bug: after advancing its local `minAvailableCommitSeq`, deleting that pathname reset the reader to `1` and accepted a cursor that had already been declared expired. The new permanent counterexample starts with a real commit, records a cursor floor, removes a loose legacy pathname, then proves the old cursor remains `TF_CURSOR_EXPIRED`.
- `ProjectControlStore.advanceCompactionCursor()` now appends a typed `CompactionCheckpoint { throughCommitSeq }` under the ordinary mutation fence and `commit.lock`; recovery validates its reserved stream, monotonicity, and `throughCommitSeq < checkpoint.commitSeq`, while public raw commits are rejected if they attempt a checkpoint beyond the existing durable tail. `loadCompactionState()` derives min/max/checkpoint sequence only from a read-only validated journal and fails closed if a root identity anchor survives a deleted control subtree.
- This is deliberately a **logical cursor** checkpoint only. It adds a new tail event and preserves every prior segment. `deleteCompactedJournalFiles()` reports eligible old paths but still deletes zero; there is no retained-prefix snapshot, physical hash-chain handoff, Receipt reachability proof, crash/power-loss proof, trusted-root solution, or retention policy closure.
- **Verdict remains NOT closed-loop GA** — P11 remains PARTIAL and P14 remains FAIL. The next P14 gate is still an approved external-witness or authenticated-repair threat model; this local checkpoint must not be presented as a solution to whole-root rollback or physical compaction.

### Round 41 — UDS hello-label binding (uncommitted)

- A real daemon/UDS red test connected as hello principal `alice`, then sent an `admit` RPC claiming `mallory` and a real Script side effect. The old transport accepted the RPC, so the command could be recorded under a different principal than the same connection's hello identity.
- UDS now rejects a non-string or unequal RPC `principal` with `TF_POLICY_DENIED` before host lookup or provider dispatch; an omitted field uses the immutable connection hello label. The permanent test proves the mismatched `admit` returns an RPC error and creates no Script marker. Client comments now distinguish a connection-bound label from an authenticated OS identity.
- **Verdict remains NOT closed-loop GA** — this only prevents per-RPC principal switching. A fresh same-UID client can still assert any syntactically valid label during hello; there is no `SO_PEERCRED`/platform peer identity, adapter credential verification, authorization policy, MCP/Pi principal binding, or public lifecycle proof. D15 and P13 remain PARTIAL.

### Round 42 — bounded UDS pending frame (uncommitted)

- A real daemon/UDS red test sent 1 MiB + 1 byte without a newline. The old server retained the incomplete string until the client timed out; it did not bound or reject the abusive connection.
- The input path now accumulates raw bytes, enforces a 1 MiB per-line ceiling for both complete and incomplete frames, replies `TF_PROTOCOL_INCOMPATIBLE`, and closes only that socket. The permanent test then admits a normal Script flow over a fresh connection, proving the daemon writer remains usable.
- **Verdict remains NOT closed-loop GA** — this is a per-line memory bound only. There is still no frame-rate, queued-RPC, or cumulative per-peer budget; malformed object-field/request-schema hardening, OS/adapter identity, holder authentication, cancellation lifecycle, P14, and all other GA rows remain open. P13 stays PARTIAL.

### Round 43 — UDS JSON shape rejection (uncommitted)

- A real daemon/UDS red test sent `null\n`. `JSON.parse` succeeded, then the old handler dereferenced `msg.type` outside its parse catch and produced an unhandled `TypeError`; malformed input could therefore fail the handler rather than receive a structured protocol denial.
- The parser now accepts only a non-array JSON object before reading protocol fields. It rejects syntactically invalid JSON, every valid non-object JSON root, missing/non-string/unknown `type`, non-string hello labels, non-string/empty RPC methods, and non-object RPC `params` with typed errors before host lookup/provider dispatch. The permanent real-daemon tests cover all valid non-object roots, malformed core envelopes, malformed hello/RPC fields, marker absence, and a subsequent normal Script admission.
- **Verdict remains NOT closed-loop GA** — this is a narrow P13 input-shape safety correction, not a complete protocol schema or transport-admission proof. Object-field validation is not exhaustive; frame-rate/queue/cumulative-budget controls, OS/adapter identity, holder authentication, UDS/CLI/MCP/Pi lifecycle parity, P14 and all other GA gates remain open. P13 and D15 stay PARTIAL.

### Round 44 — UDS duplicate-hello rebind rejection (uncommitted)

- The Round-41 connection-label claim had a concrete bypass: after hello as `alice`, the same socket could send another valid hello as `mallory`; the old handler overwrote `conn.principal`, so an `admit` carrying `principal: mallory` passed the per-RPC equality check and could dispatch a Script side effect.
- A completed handshake is now one-shot. A second hello returns `hello-error` / `TF_PROTOCOL_INCOMPATIBLE` without changing the first label. The real-daemon adversarial test then sends the mismatched RPC, asserts `TF_POLICY_DENIED` and marker absence, and proves a fresh connection can still admit a normal Script flow.
- **Verdict remains NOT closed-loop GA** — this repairs only a same-socket state-machine bypass. The first hello remains client-asserted; no OS peer credential, adapter credential, holder authentication, authorization/grant policy, frame-rate/queue budget, full protocol schema, or public lifecycle proof exists. P13 and D15 remain PARTIAL; P14 remains FAIL.

### Round 45 — UDS open-peer admission cap (uncommitted)

- A real daemon/UDS red test opened 64 idle peers and then one more. Before the cap, the extra peer received no protocol response and remained open until the test timeout, so a same-UID local process could consume unbounded idle socket state.
- The server now tracks accepted peers and returns `error` / `TF_CAPACITY_EXCEEDED` to the 65th connection before allocating parser state. It releases the slot on close and has a per-socket error listener so a reset while writing a rejection cannot turn into an unhandled EventEmitter crash. The permanent test holds the full cap, validates the excess response, resets another denied peer, closes the held peers, and proves a fresh Script admission succeeds.
- **Verdict remains NOT closed-loop GA** — 64 is a bounded ceiling, not complete transport admission. An accepted pre-hello peer now has a five-second absolute deadline, but there is still no rate limit, queued-RPC/cumulative byte budget, fair scheduling, OS/adapter identity, holder authentication, or full protocol schema. P13 and D15 remain PARTIAL; P14 remains FAIL.

### Round 46 — UDS pre-hello deadline (uncommitted)

- A real daemon/UDS red test opened one idle local peer and sent no bytes. With no deadline, it produced no response within `UDS_HELLO_DEADLINE_MS + 1500ms`; that peer could retain an accepted admission slot until it chose to close.
- Every accepted peer now starts a **five-second absolute** hello timer. If a successful first hello has not completed by the deadline, the server returns `error` / `TF_PROTOCOL_INCOMPATIBLE` and closes only that socket. A successful hello clears the timer; peer close also clears it and releases the slot. The permanent real-daemon test waits for the deadline response, then admits a fresh real Script flow to prove the writer remains usable.
- **Verdict remains NOT closed-loop GA** — this bounds one idle pre-authentication socket, not transport admission or authentication. There is still no byte-rate, queued-RPC, cumulative-budget, fair-scheduling, full request-schema, OS/adapter credential, holder-identity, authorization/grant, or MCP/Pi lifecycle proof. P13 and D15 remain PARTIAL; P14 remains FAIL.

### Round 47 — P14 repair-authority decision audit (uncommitted)

- The current real `store-recovery` counterexample still passes as a **negative proof**: it commits an event, copies the control subtree plus project-root anchor, commits a later event, restores the coherent old pair, and reopens with the older history. It is not a green P14 behavior test and does not make rollback acceptable.
- Independent ADR review found a decision hazard in a vague “authenticated repair” phrase: a signed or journaled repair record stored only under the same project root can be rolled back with every other local input. It therefore cannot establish freshness. The P14 ADR and closure goal now make the dependency explicit: repair/rebind must be backed by an external or OS authority that the same restorer cannot rewind, otherwise normal execution must fail closed and product must select non-GA/local-only scope.
- The ADR now requires an approved decision record (authority, protected tuple, availability, clone/rebind, credentials, audit, privacy, migration), and a future real-path contract covering whole-root restore, authority loss/replay, CAS races, crash points, repair replay, legacy migration, and MCP/Pi/CLI/taskflowd parity before implementation begins.
- **Verdict remains NOT closed-loop GA** — no trust model was selected, no witness or repair protocol was implemented, and no P14 row was promoted. The current local engine remains P14 FAIL; full local gates only verify the documentation/code tree, not rollback freshness.

### Round 48 — existing-header root-anchor loss fail-closed (uncommitted)

- A real `store-recovery` red test initialized a ControlStore, retained `header.json`, removed only `.taskflow-control.anchor.json`, and reopened. The old mutable open accepted the header and continued; it treated identity-evidence deletion as if an unanchored legacy store were safe to observe.
- Every existing-header path now calls one shared `requireProjectRootAnchor` guard before identity-policy resolution. Mutable open, read-only attach, `rebind`, and `new-identity` all return `TF_DURABILITY_FAILED` when the anchor is absent, leave the existing header untouched, and do not mint a replacement anchor or identity. A separate focused P1 identity-policy run preserves explicit clone `new-identity` and same-domain `rebind` behavior when the existing anchor is present.
- **Verdict remains NOT closed-loop GA** — this closes only deletion of one root-local evidence file while the header remains. It deliberately leaves legacy header-only stores unavailable until an explicit approved migration exists, and it cannot distinguish a whole-root rollback that restores both header and anchor. P14 remains FAIL pending the external/OS trust decision, migration, compaction, crash/no-follow/public-entrypoint coverage, and the rest of the GA matrix.

### Round 49 — C4 cancellation authority/observation containment (uncommitted)

- New adversarial red tests showed that a mutation-fence loss after `CancelRequested` and before `signalling`, and a loss before terminal cancellation settlement, escaped `ControlHost.cancel()` as an uncaught `SingletonAuthorityError`. A third red test showed that a fence loss while persisting `ambiguous` was silently flattened to `TF_RECONCILE_REQUIRED`; provider and ControlStore observation failures after a provider cancel could also escape before an ambiguity record was made.
- `ControlHost.cancel()` now translates all tested pre-intent/intent/signal/ambiguity/settlement fence boundaries into a structured `TF_AUTHORITY_REVOKED` response with the retained durable snapshot. Before `CancelRequested` it is explicitly retryable with `sideEffects:none`; once a request exists it is reconcile-only. An unsuccessful quiescence observation is fail-closed as `ambiguous` / `TF_RECONCILE_REQUIRED`, retaining capacity and no Receipt. The C4 suite has real Script checks for the pre-signal and fence boundaries, plus adversarial terminal/observation providers to reach host paths that raw Script intentionally cannot safely terminalize.
- **Verdict remains NOT closed-loop GA** — these are test-only injected fences, not an authorized cross-process stale-writer replacement or cancel-recovery protocol. There is no public cancel reconciliation owner, no containment-backed terminal observation, no generic-provider capability contract, no UDS/CLI/MCP/Pi lifecycle or full error-envelope proof, and no 32×100 pressure evidence. P13/P15 remain PARTIAL and P14 remains FAIL.

### Round 50 — C7 provider outcome / dispatch race audit (uncommitted)

- Bare provider `cancelled` reports are now conservatively routed through reconciliation on direct cancel, bounded reconcile, poll, and collect paths; they cannot mint a terminal run, Receipt, or normal capacity release. A collect transport fault likewise becomes retained `unknown/reconciling` evidence instead of an uncaught host exception. Permanent local tests also cover late scheduler completion after cancel, cancel-before-park orphan prevention, an ordinary `DispatchIntentRecorded → cancel → beforeProviderSubmit` barrier, a pre-scheduler raw-emit race, and the corresponding real Script native-approval continuation barrier.
- This is intentionally a **local containment slice only**. The new durable lease check suppresses the specific microtask where cancel commits before `beforeProviderSubmit`, but it is not a shared cancel-vs-side-effect linearization primitive: a cancel can still commit after the last lease read and before a provider operation. A local fence cannot prove asynchronous remote submission; Script requires a shared synchronous run/attempt fence around exact re-read, reserve, spawn, and acknowledgement, while remote providers require provider-enforced fencing/idempotency or a fail-closed capability gate.
- A separate adversarial review also found that cancel and quiescence still use the script-provider alias rather than durable `providerName`. A live LLM handle can therefore be cancelled/observed through the wrong provider; the false `isLive=false` result can park a still-live run and release its slot. Until a fail-closed durable provider resolver validates `(providerName, attempt identity, runId, handle)` on cancel/isLive/reconcile/park and survives reopen, C7 is **PARTIAL / GA blocker** and D38/P13 must not be promoted.
- **Verdict remains NOT closed-loop GA** — final local commands may be green, but they do not repair P14 whole-root rollback, the C/S atomic lower bound, incorrect multi-provider routing, public cancellation lifecycle, dual entry paths, or any other mandatory matrix row.

### Round 51 — C7 durable routing and synchronous submission-fence slice (uncommitted)

- The previous wrong-provider counterexample is closed only at its tested host boundary. `ControlHost` now builds a provider registry by durable name and derives a cancel/quiescence route from the current Run, matching Continuation, active attempt, continuation version, phase id, provider name, and handle. The cancel request snapshots that route identity before any provider side effect. Unknown provider, missing continuation/attempt, or mismatched handle now becomes durable `unknown/reconciling` evidence; it never falls back from an opaque handle to Script. New LLM-versus-Script tests prove that `parkForApproval()` consults the durable LLM provider, a reopened host sends cancel only to that LLM provider, and a reopened host with the LLM provider missing makes zero Script calls. Two same-name foreign-record tests prove that the later cancel and pending-dispatch recovery paths do not signal/reconcile a handle whose own `loadHandle()` record belongs to another Run.
- The former same-process `read(no C) → C → S` reentrancy is now covered for a provider that consumes the synchronous `submissionFence`: `ProjectControlStore.withProviderSubmissionFence()` shares the mutation/commit lock with Run mutations, and `ControlHost` keeps an in-process pending fence so a reentrant `cancel()`/park waits until the submit operation returns. Script and host-LLM submit paths consume that fence. The permanent red/green fixture invokes `host.cancel()` synchronously from inside the provider fence and proves the cancel is not durable before the protected side effect.
- An independent re-review found a stronger, earlier C7-B failure that the new later guards cannot repair. `ExecutionProvider.submit()` may return `foreign-handle`; scheduler calls `onDispatchAcknowledged` with that value, host journals it, and scheduler then calls `poll/collect` without a `loadHandle()` ownership check. A controlled current-tree reproduction uses a same-name provider whose `loadHandle(foreign-handle)` says `other-run:other-phase` while `poll()` returns completed. It actually produces `pollCalls=1`, `completed/terminal`, `foreign-job-output`, a Receipt, and a released reservation. Thus the patch protects later cancel/recovery calls but still permits a foreign job to terminalize the current Run. The next permanent red test must require `poll/collect=0`, `unknown/reconciling`, no Receipt, and retained capacity; the implementation must verify the exact provider-owned record before `DispatchAcknowledged` and terminal observation.
- This is **not** a C7 promotion. The primitive does not make an asynchronous remote/generic provider honor the fence, does not prove a cross-process cancel/submission-acknowledgement handoff, and does not supply a provider-side containment/idempotency/recovery contract. The durable route tuple is newly written but schema version/migration/quarantine policy for pre-slice scratch journals is still absent; reconcile/repair and daemon/UDS/CLI/MCP/Pi lifecycle parity are also unproven. Therefore C7, D6, D38, P13, P15 and P16 remain **PARTIAL**, while P14 remains **FAIL**.
- Final Round-51 local checks on the dirty `e11b8210` worktree: focused `provider-cancel-containment.test.ts` **16/16**; `pnpm test` **2234/2234**; `pnpm run test:control` **172/172**; `pnpm run test:daemon` **15/15**; `pnpm run test:cli` **5/5**; `pnpm run typecheck` and `git diff --check` exit 0. The Round-51 manifest binds this matrix, closure goal, review, source audit and validation logs. These are local, uncommitted evidence only; no push, PR, tag, publish, release, or remote CI occurred.
- **Verdict remains NOT closed-loop GA.** P14 whole-root rollback alone forbids GA. C7-B dispatch-owned-handle validation, C7-C provider capability, C7-D durable-state migration, C7-E public lifecycle, the dual entry paths, real 12-phase semantics, approval breadth, clean packed consumer, clean candidate SHA, and authorized remote CI remain separate mandatory gates.

### Round 52 — C7-B static ownership containment / C7-C lower bound (uncommitted)

- The permanent foreign-handle red test is now green only for the narrow static
  case: a new foreign or missing-record handle is never acknowledged or
  observed; a historical foreign acknowledgement is not re-observed. Both
  become durable `unknown/reconciling + needsOperator`, with no Receipt and no
  normal capacity release.
- A separate permanent C7-C lower-bound test deliberately preserves the
  stronger failure: the provider returns matching records for both scheduler
  checks, rebinds the opaque handle immediately after the second check, and
  the current `poll` path false-terminalizes with a foreign output, Receipt,
  and released reservation. Passing that test documents the defect; it cannot
  be counted as a safety PASS.
- Final local dirty-tree checks: combined focused C7 suite **28/28**; `pnpm
  test` **2238/2238**; `pnpm run test:control` **176/176**; `pnpm run
  test:daemon` **15/15**; `pnpm run test:cli` **5/5**; `pnpm run typecheck`
  and `git diff --check` exit 0. The independent review and SCRATCH manifest
  bind the exact scope. No push, PR, tag, publish, release, or remote CI
  occurred.
- **Verdict remains NOT closed-loop GA.** The next C7 action is a provider-side
  capability/incarnation contract, not another host-side ownership reread;
  P14 remains independently FAIL.

### Round 53 — P14 predictable temporary-path symlink containment (uncommitted)

- A Unix real red test fixes `Date.now()`, plants the old predictable
  `<target>.<pid>.<millisecond>.tmp` as a symlink to an external-root sentinel, and
  invokes `writeFileAtomic()`. The old `"w"` path follows that symlink: it
  overwrites external bytes and publishes the target as a symlink.
- `writeFileAtomic()` now creates a UUID-named temporary file with exclusive
  `"wx"` / `0600` before writing. A controlled current-name collision test
  plants a symlink immediately before that `openSync`; `EEXIST` is retried and
  no external bytes change. Separate write+close, fsync+close, close-only, and
  rename fault tests retain the causal error and leave no uniquely created temp
  entry.
- A permanent lower-bound counterexample replaces the newly opened UUID source
  entry with a symlink immediately before `renameSync`. The current helper
  returns success and publishes that symlink. Passing this test records an
  unsafe behavior; it is not a safety PASS and must change only with a
  directory-FD/fd-to-publish primitive.
- This is a local pathname hardening only. It does not prove an OS-level
  directory-FD no-follow protocol against parent, source, or destination
  replacement; cross-platform filesystem behavior, directory-fsync I/O
  handling, whole-root freshness, repair authority, power-loss durability, and
  physical compaction remain open. **P14 stays FAIL; no matrix row is promoted
  to GA PASS.**

---

### Round 60 — coherent journal observation is a local G0 repair, not a matrix promotion

- A complete raw-TAP parallel and serial rerun now records **2281/2281** pass
  for this exact dirty checkout. This repairs the previous evidence-preservation
  defect (the earlier failed run retained only a tail), but it is local
  repeatability evidence only; it neither deletes the historical failure nor
  proves production pressure behavior.
- The immediate G0 source repair prevents native-approval replay from composing
  an old rebuildable `Run` projection with a newer journal continuation. A
  journal-authoritative tuple is now observed under `commit.lock` for writers;
  read-only attaches fail closed if the journal anchor changes during the
  observation. Pre-submit intent, reconciling, and ownerless admitted states no
  longer disclose `ok:true`.
- Permanent local tests cover the stale projection/new journal-tail case,
  pre-submit/reconciling disclosure, post-open journal symlink, and a direct
  anchor-advance observation race. The direct race uses a test-local Node fs
  seam; no public production test callback remains.
- This does **not** create a first-dispatch recovery owner or improve the
  external trust root. It also introduces/retains an O(history) journal parse
  under the mutable status lock, which is a P16-3 availability debt. Therefore
  **no §23, D, or P status changes**: in particular §23.6/§23.7/§23.11/§23.12,
  D16/D24/D30/D34/D37/D38, effective P13, P14, P15, and P16 remain FAIL.

---

### Round 75 — reservation epoch derives from the held writer capability; no P13/P16 promotion

- In the isolated dirty `codex/p16-g1-v2-admission` worktree at
  `e11b82109e04f69e56a37aceb3f78934f822ad14`, a true singleton writer could
  previously call `reserve({ coordinatorEpoch: lockEpoch - 1 })` and persist
  that caller-selected value. A raw no-owner caller could likewise write a
  nonzero lease epoch. That made durable epoch fields unsuitable as evidence
  of the writer which actually held the singleton capability.
- The coordinator now rejects legacy/unknown `reserve` fields, derives the
  reservation epoch while the opaque singleton capability and its durable
  fence are held, and requires `setLease` to use that same derived epoch.
  Project-local, raw no-owner, and explicit `skipSingleton` non-GA routes
  persist the explicit unauthoritative sentinel `0`; they are not a GA
  authority protocol. Native approval and generic admission no longer supply
  a caller-selected epoch.
- The red proof was a focused `stale-lock` run with **11 pass / 1 fail**, whose
  persisted epoch was `lockEpoch - 1` rather than the live lock epoch. Final
  local checks were: focused `stale-lock` **12/12**; `store-recovery`
  **54/54**; `closed-loop` **20/20**; `pnpm run typecheck` exit 0;
  `pnpm run test:control` **272/272**; `pnpm run test:daemon` **21/21**;
  `pnpm run test:cli` **4/4**; exact `pnpm test` **2339/2339** in
  116.858 s; `pnpm run test:pack` exit 0 (12 packages, 28 explicit imports,
  68 wildcard exports, bins); and `git diff --check` exit 0. An initial full
  control run exposed one cross-process helper still using the rejected legacy
  field; it was updated before the final green run.
- These outputs were observed in the session only. There is no new clean-SHA
  raw-log/manifest SCRATCH packet, and no staging, commit, push, tag, publish,
  or remote CI occurred. **No status changes:** effective P13 remains FAIL;
  P14/P15/P16 remain FAIL; §23.6, D24, and D30 remain FAIL. The remaining
  binding is caller-shaped, C3/`FirstDispatchOwner` is still not in the real
  lifecycle, whole-root rollback has no external trust decision, and capacity
  pressure/recovery proof is absent.

---

### Round 76 — bounded fresh-singleton pressure evidence; no P13 authority promotion

- In the same isolated, already-dirty `codex/p16-g1-v2-admission` worktree at
  `e11b82109e04f69e56a37aceb3f78934f822ad14`, `pnpm run
  test:stress-singleton` now runs a fixed **100 freshly isolated homes × 32
  concurrent processes**. Each round asserts exactly one writer, 31 attaches,
  one shared `holderId` and `fencingEpoch`, and singleton-lock cleanup only
  after the writer releases. The observed bounded marker was
  `{result:"pass", rounds:100, workersPerRound:32, acquisitions:3200,
  elapsedMs:155317}`.
- This is deliberately a **fresh-home exclusion** test, not an authority
  replacement claim: it does not crash a writer, take over a stale lock,
  replace a pathname, exercise Windows semantics, or prove all host entrypoints
  preserve a fence. The permanent effective-P13 lower bound remains unchanged:
  a pathname replacement cannot be safely compare-and-deleted without an
  OS-backed holder-identity protocol.
- Current local session checks were `pnpm run typecheck` exit 0;
  `pnpm run test:control` exit 0 (71.037 s); `pnpm run test:daemon` exit 0
  (21/21, 20.026 s); `pnpm run test:cli` exit 0 (4/4, 10.932 s); `pnpm test`
  exit 0 (113.912 s); `pnpm run test:pack` exit 0 (94.560 s); and
  `git diff --check` exit 0. They are unclean local executions, not candidate
  SHA evidence.
- The result marker and command exit records were temporary session files, not
  a new committed raw-log/SHA-256-manifest SCRATCH packet. No status changes,
  staging, commit, push, tag, publish, remote CI, or GA assertion occurred.
  Effective P13 remains **FAIL**; P14/P15/P16 and their dependent §23/D rows
  remain **FAIL**. In particular, this does not create P16 C3 or a durable
  `FirstDispatchOwner`, and it does not supply the missing P14 external trust
  root / authenticated-repair decision.

---

### Round 77 — release/fence require the opaque writer capability; no P13 promotion

- In the same isolated, already-dirty `codex/p16-g1-v2-admission` worktree at
  `e11b82109e04f69e56a37aceb3f78934f822ad14`, the red regression showed that
  a caller could read `singleton.lock`, deserialize the exact current owner
  record, and pass it to public `releaseSingleton` to unlink the live writer.
  The same raw `SingletonLockInfo` could be supplied to the publicly exported
  `withSingletonMutationFence`. Those are capability-boundary failures even
  though the record contains a fencing epoch.
- `releaseSingleton` now accepts only the private WeakMap-minted
  `SingletonMutationAuthority`; a copied JSON record throws
  `TF_AUTHORITY_REVOKED` and leaves the lock bytes unchanged. The raw-record
  fence is module-private. `ControlHost` no longer treats an observed writer
  record as a mutation capability, and `taskflowd` passes the same opaque
  capability to its mounted-host fence, authority observation, UDS check, and
  release path. A permanent regression asserts both the copied-record denial
  and absence of the raw fence from the root and direct singleton module
  surfaces.
- Local checks after the repair: `pnpm run typecheck` exit 0; focused
  `stale-lock.test.ts` **14/14**; focused `daemon.test.ts` **10/10**; official
  `pnpm run test:control`, `pnpm run test:daemon`, and `pnpm run test:cli`
  each exit 0; `pnpm test` exit 0; `pnpm run build` exit 0;
  `pnpm run test:pack` exit 0; and `git diff --check` exit 0. They are dirty,
  local executions with temporary exit records only—not clean-SHA artifacts.
- **No status change:** this protects an in-process/API-level serialized-lock
  forgery. It cannot atomically compare-and-unlink a pathname after a hostile
  replacement, does not authorize stale takeover, and provides no Windows or
  full-entrypoint proof. Effective P13 remains **FAIL**; P14/P15/P16 remain
  **FAIL**; no new SCRATCH packet, staging, commit, push, tag, publish, or
  remote CI occurred.

---

### Round 78 — implicit ownerless global coordinator mutation is denied; no P13 promotion

- In the same isolated, already-dirty
  `codex/p16-g1-v2-admission` worktree at
  `e11b82109e04f69e56a37aceb3f78934f822ad14`, the red regression opened a
  fresh global `UserCoordinatorStore` with no singleton owner and called
  `reserve()`. The call succeeded, created `state.json` and its anchor, and
  persisted epoch `0`. The focused `stale-lock` run was therefore **14 pass /
  1 fail**, failing only because this raw operation did not throw
  `TF_AUTHORITY_REVOKED`.
- The normal global mutation entry now requires the private
  `SingletonMutationAuthority`. If no capability is present and the caller has
  neither a project-local `baseDir` nor the deliberately named
  `allowUnfencedMutationForExplicitNonGaMode`, it throws
  `TF_AUTHORITY_REVOKED` before it creates `state.json` or the initialization
  anchor. `withNoSingletonMutationAuthority` was removed from
  both the package-root and direct singleton public surfaces. Project-local
  standalone storage and explicitly selected non-GA embedded/test fixtures
  retain their observable epoch-`0` behavior; they are not silently upgraded
  into normal global authority.
- **Historical qualification:** Round 79 below corrects the unqualified
  `baseDir` wording in this Round 78 account. A `baseDir` that resolves to the
  canonical user-global coordinator is not project-local and must not select
  the standalone epoch-`0` route.
- The permanent regression asserts both denial-with-no-files-created and the
  still-explicit non-GA escape hatch. Raw fixture callers in recovery,
  closed-loop, migration, force-release, daemon, and multi-process tests now
  name that escape hatch at their suite/helper boundary, so their direct
  coordinator setup cannot be misread as normal writer evidence.
- Post-repair local checks in that dirty worktree: `pnpm run typecheck` exit
  0; focused `stale-lock.test.ts` **15/15**; `pnpm run test:control` **275/275**;
  `pnpm run test:daemon` **21/21**; `pnpm run test:cli` **4/4**; `pnpm test`
  **2342/2342**; `pnpm run build` exit 0; `pnpm run test:pack` exit 0 (12
  packages, 28 explicit imports, 68 wildcard exports, bins); and `git diff
  --check` exit 0. These are dirty local executions, not a candidate-SHA,
  clean-consumer, or remote-CI packet.
- **No status change:** this removes a default API-level no-owner write path,
  but it does not resolve P13's pathname-replacement/stale-takeover lower
  bound, P14's external freshness/repair decision, P15's generic approval
  saga, or P16's C3/`FirstDispatchOwner`. Effective P13/P14/P15/P16 remain
  **FAIL**; §23.2 remains **PARTIAL**. No SCRATCH SHA-256 manifest was
  synthesized from this prose, and no staging, commit, push, tag, publish, or
  remote CI occurred.

---

### Round 79 — canonical global `baseDir` alias is denied; no P13 promotion

- **Observed red boundary.** After the Round 78 default denial, a fresh
  `TASKFLOW_HOME` could still call
  `openUserCoordinatorStore(env, { baseDir: coordinatorDir(env) }).reserve()`.
  The optional `baseDir` selected the project-local standalone branch, yet
  named the same user-global ledger. It therefore initialized the global
  `state.json`/anchor and persisted an unauthoritative epoch-`0` reservation
  without a writer capability. The permanent `stale-lock` regression was
  extended with that exact canonical alias; before the repair the focused run
  was **14 pass / 1 fail**, because no `TF_AUTHORITY_REVOKED` was raised.
- **Narrow repair.** `openUserCoordinatorStore` now computes
  `globalCoordinatorDir = coordinatorDir(env)` before choosing its storage
  route. If a supplied `baseDir` has the same `path.resolve` spelling as that
  canonical global directory, it throws `SingletonAuthorityError`
  (`TF_AUTHORITY_REVOKED`) before opening, locking, or creating the ledger.
  A non-alias project-local `baseDir` and the deliberately named explicit
  non-GA route retain their existing epoch-`0` behavior. This is a route
  classification guard, not a filesystem-equivalence protocol: it makes no
  new claim for symlink aliases, case-folding, no-follow races, stale takeover,
  or Windows.
- **Permanent proof and local gates.** The single regression now denies both
  `openUserCoordinatorStore(env).reserve()` and the canonical-`baseDir` form,
  verifies that neither creates the global `state.json` or anchor, then shows
  the visible explicit non-GA exception remains epoch `0`. In the same dirty
  isolated worktree, post-repair checks were: focused `stale-lock.test.ts`
  **15/15**; `pnpm run typecheck` exit 0; `pnpm run test:control`
  **275/275**; `pnpm run test:daemon` **21/21**; `pnpm run test:cli` **4/4**;
  `pnpm test` **2342/2342**; `pnpm run build` exit 0; and `pnpm run test:pack`
  exit 0 (12 packages, 28 explicit imports, 68 wildcard exports, bins).
  These are dirty local executions, not a clean candidate SHA, raw-log
  manifest, clean-consumer packet, or remote CI proof. The existing
  `round60-artifacts.manifest.sha256` verification now exits 1: its two
  document inputs (`0.3.0-ga-traceability-matrix.md` and
  `0.3.0-ga-closure-goal.md`) no longer match, while its other 13 inputs still
  match. It is historical evidence, not a current packet; it was not rewritten
  to mask that fact.
- **No status change.** This closes one additional API route to the global
  epoch-`0` mutation branch. It supplies neither the OS-backed
  holder-identity compare-and-delete needed for P13 nor P14's external
  freshness/authenticated-repair decision, P15's generic approval saga, or
  P16's C3/`FirstDispatchOwner`. Effective P13/P14/P15/P16 remain **FAIL** and
  §23.2 remains **PARTIAL**. No SCRATCH SHA-256 evidence packet was created;
  no staging, commit, push, tag, publish, or remote CI occurred.

---

### Round 80 — provenance audit: isolated capability repairs are not code on `feat/0.3.0`

- **Observed checkout split.** The main worktree
  `/Users/beta/work/personal-projects/pi-taskflow` is on `feat/0.3.0` at the
  same base `e11b82109e04f69e56a37aceb3f78934f822ad14`, but is a separate dirty
  tree from the isolated `codex/p16-g1-v2-admission` worktree used for Rounds
  77–79. The main worktree does **not** contain those source changes:
  `withSingletonMutationFence` is still publicly exported,
  `releaseSingleton` still accepts a serialized `SingletonLockInfo`, and its
  global `openUserCoordinatorStore` still accepts a caller-supplied
  `coordinatorEpoch` with no opaque capability requirement.
- **Fresh temporary-home reproductions on the main worktree.** A direct
  `openUserCoordinatorStore(env).reserve({ coordinatorEpoch: 0 })` succeeded
  on a new `TASKFLOW_HOME`, returned a reservation at epoch `0`, and created
  the canonical global `state.json`. Separately, a writer was acquired, its
  public lock file read and JSON-parsed, and
  `releaseSingleton(copiedRecord, env)` removed that live lock. Both commands
  exited 0 and cleaned their temporary homes. They are deterministic current
  counterexamples, not merely a missing test count.
- **Main-worktree local checks.** This session's first `pnpm run test:control`
  ended **217/218**, exit 1; an immediate exact rerun ended **218/218**, exit
  0. The failure's complete TAP diagnostic was not retained, so the later
  green run cannot erase the observed non-repeatability. `pnpm run test:daemon`
  passed **16/16**, `pnpm run test:cli` passed **5/5**, and `pnpm test` passed
  **2281/2281**. These dirty-tree test results do not exercise away the direct
  public-API counterexamples above and are not a clean/serialized evidence
  packet.
- **Evidence consequence.** The Rounds 77–79 green results prove only the
  isolated dirty source slice; they are not test evidence for the current
  `feat/0.3.0` worktree and cannot support a branch candidate, package, or GA
  assertion. No SCRATCH log or manifest was minted for these interactive
  reproductions, no files were staged, and no remote action occurred.
- **Required next integration packet.** Before any status discussion, make a
  deliberate semantic merge in one chosen worktree: opaque capability minting
  and release/fence API; ControlHost/bootstrap/daemon propagation; global
  coordinator capability enforcement plus canonical-`baseDir` denial; and
  explicitly labelled non-GA fixture routes. Retain the serialized-release,
  raw-global, and canonical-alias tests on that same integration tree, then
  rerun focused → owning → full/control/daemon/CLI → build/pack checks there.
  This integration would still be only P13 narrowing; it cannot promote
  P13/P14/P15/P16 or GA.

---

### Round 81 — pure migration-pair classifier now witnesses M2/M4/M5 chains; still no migration runtime

- **Scope and starting red cases.** This is code only in the separate dirty
  `/Users/beta/work/personal-projects/pi-taskflow-p16-g1-v2` worktree on
  `codex/p16-g1-v2-admission`, based on
  `e11b82109e04f69e56a37aceb3f78934f822ad14`; it is not source on the actual
  dirty `feat/0.3.0` worktree. The first red case showed that a syntactically
  valid but unwitnessed 64-hex C2 predecessor made the pure
  `assessMigrationPair()` return `migration-only/M4` (**9/10** focused tests
  passed). After that narrow repair, independent review found four more red
  cases (**12/16** passed): non-array coordinator history threw instead of
  quarantining; an array-like project history was accepted; C3-active could
  replace the C3-pending M2 reference set; and one selected P2-active project
  could make a two-project C3-active pair appear future-runtime-ready while
  the other project had no M4 witness. A final state-table red case was
  **17/18**: C2/M2 accepted residual P2/C3 witness input and regressed to
  `migration-only/M2`.
- **Narrow pure-contract repair.** The classifier now accepts predecessor
  bytes only as strict arrays, recursively hashes and parses C2 -> C3-pending
  -> C3-active, preserves immutable lineage and timestamps, rejects residual
  witness sets in legacy/C2/C3-pending states, and requires C3-active to keep
  the exact canonical C3-pending M2 refs. A P2-active envelope must carry its
  exact P2-preparing witness whose C2 commitment is the witnessed C3 lineage;
  C3-active additionally requires a complete, canonical-order
  `activeProjectEnvelopeWitnesses` set, one verified P2-active chain for every
  frozen project, and an exact match for the selected project pair. The
  permanent tests include both the two-project complete positive and missing
  project / substituted-current / substituted-ref negatives. This gives a
  pure classifier a fail-closed lower bound; it grants no mutation capability.
- **Post-repair local evidence in that target worktree.** Focused
  `migration-contract.test.ts` is **18/18**, `pnpm run typecheck` exits 0,
  `pnpm run test:control` is **284/284**, `pnpm run test:daemon` is **21/21**,
  `pnpm run test:cli` is **4/4**, and `pnpm test` is **2351/2351**. `pnpm run
  build`, `pnpm run test:pack` (12 packages, 28 explicit imports, 68 wildcard
  exports, bins), and `git diff --check` all exit 0. They are repeatable local
  feedback on a dirty isolated tree, not a clean candidate, raw-log/SHA-256
  SCRATCH packet, or remote CI proof.
- **Hard lower bound.** A source search excluding the classifier and its test
  found no production caller of `assessMigrationPair()` (exit 1/no matches).
  The new histories and active witness set are caller-supplied JS values; they
  are not an authenticated root, a signed/externally witnessed record, an
  append-only store, a locked source snapshot, or a recovery authority. The
  actual paths still lack durable M0--M5 writers/readers, M1 mutation fencing,
  all-entrypoint zero-effect proof, rollback/deletion resistance, and the P14
  trusted-root/authenticated-repair decision. Therefore this does not promote
  P13, P14, P15, P16, §23.2, any D/P row, wire freeze, candidate status, or
  GA. No SCRATCH evidence manifest was created; no file was staged, committed,
  pushed, tagged, published, or sent to remote CI.
- **Required successor, not optional polish.** A future authorized migration
  authority must load canonical raw P/C records and complete histories under
  one holder/fence, bind them to a P14-approved external/non-rollbackable
  trust root and M0 snapshot, write/recover M1--M5 durably, and invoke this
  classifier only as one verifier inside that protocol. It must prove every
  crash prefix, stale/authorized successor, old-writer barrier, all-project
  activation, deletion/replacement/rollback, and public-entrypoint behavior
  before any status may change.

---

### Round 82 — P13 opaque-capability path is locally coherent in the isolated tree; current branch remains directly bypassable

- **Target source-call audit, not a branch integration claim.** In the separate
  dirty `/Users/beta/work/personal-projects/pi-taskflow-p16-g1-v2` worktree on
  `codex/p16-g1-v2-admission`, the only non-test `releaseSingleton` calls are
  `ControlHost.close()` and daemon shutdown/failure cleanup, and each passes
  `singleton.mutationAuthority`. The only raw daemon
  `openUserCoordinatorStore(env)` call is `assertReadable()` before singleton
  acquisition; its mutating mounted-host path propagates the same opaque
  capability through bootstrap, `ControlHost`, the coordinator, the durable
  project fence, and UDS writer-authority predicate. The normal global
  coordinator branch rejects missing/forged capabilities before initialization;
  the standalone and explicitly named non-GA paths remain visibly separate.
  This is a static local source graph plus tests, not proof that every public
  product entry point has been integrated or that the target worktree is a
  candidate.
- **Focused target behavior.** A fresh direct run of
  `stale-lock.test.ts`, `modes-and-stress.test.ts`, and `daemon.test.ts` passed
  **30/30** in that target tree. It covers exactly-one-writer contention,
  dead-owner fail-closed, serialized lock denial, raw/no-owner global
  initialization denial, forged capability denial, canonical global
  `baseDir` alias denial, epoch loss before direct/UDS mutation, and the
  daemon's opaque cleanup path. It supplements, but does not replace, Round
  81's target-tree 284/284 control and 2351/2351 full-suite results.
- **Fresh main-branch counterexample.** On the actual dirty
  `feat/0.3.0` worktree at `e11b82109e04f69e56a37aceb3f78934f822ad14`, an
  isolated temporary-home script again executed
  `openUserCoordinatorStore(env).reserve({ coordinatorEpoch: 0 })`, then read
  the exact `singletonLockPath(env)` bytes and called
  `releaseSingleton(JSON.parse(bytes), env)`. It exited 0 and reported
  `{"rawReserveEpoch":0,"copiedRelease":true}`. The temporary home was
  removed afterwards. Thus the source being evaluated for GA still permits
  both bypasses; target-tree green tests cannot be attached to this branch.
- **Unchanged safety lower bound.** The opaque capability only stops a caller
  from treating readable lock JSON or a numeric epoch as authority. It does not
  provide an OS-backed holder identity, compare-and-replace/delete primitive,
  authenticated UDS peer, safe stale-owner recovery, no-follow publication
  protocol, Windows equivalence, or P14 freshness root. `P13` therefore stays
  **FAIL** even after a deliberate semantic integration, and P14/P15/P16 stay
  independently **FAIL**.
- **Required next gate.** Do not copy the isolated diff into the dirty main
  worktree mechanically. First obtain the P14 external-witness versus
  externally backed repair versus non-GA-scope decision; then choose one
  integration worktree, preserve the three P13 direct-bypass regressions, and
  prove the capability chain across direct host, daemon/UDS, CLI/MCP/Pi and
  all shutdown/error paths. The resulting work must still keep stale recovery
  fail-closed until a separately selected OS holder/replacement protocol is
  implemented and adversarially proven. No SCRATCH manifest, stage, commit,
  push, tag, publish, or remote CI action occurred in this round.

---

### Round 83 — default MCP background fails closed in the isolated tree; Pi remains an independent 0.2 ingress

- **Actual red counterexample.** In the dirty isolated
  `/Users/beta/work/personal-projects/pi-taskflow-p16-g1-v2` worktree on
  `codex/p16-g1-v2-admission`, a new default-environment MCP test first failed:
  `taskflow_run({mode:"background"})` returned success, spawned the detached
  runner, and reported `↻ taskflow started in background`. The code itself
  documented that this path always called `launchMcpBackgroundRun`, which uses
  a separate `.pi` run store even while `TASKFLOW_CONTROL_PLANE` defaults on.
  This is a direct D18/D21/one-ledger violation, not a missing test label.
- **Narrow safety repair.** Before constructing a run state or launching a
  detached process, the isolated MCP server now checks the default
  `controlPlaneEnabled()` result. When enabled it returns an explicit tool error
  explaining that daemon-backed background execution is not implemented and
  that legacy `.pi` state would not share ControlStore/authority/Receipt. The
  legacy detached protocol remains reachable only after the explicit emergency
  `TASKFLOW_CONTROL_PLANE=0` opt-out; its compatibility suite now declares that
  opt-out at file scope instead of silently testing it as the product default.
  This is fail-closed admission, not an implementation of background
  run/status/wait/cancel/approval.
- **Evidence in that isolated tree.** The first red test failed exit 1 with the
  old start message. Post-change, the default no-write regression passed 1/1;
  the explicit-opt-out legacy background suite passed 14/14; the owning MCP
  suite passed **31/31**; `pnpm run typecheck` passed; `pnpm test` passed
  **2352/2352**; `pnpm run test:control` passed **284/284**;
  `pnpm run test:daemon` passed **21/21**; `pnpm run test:cli` passed **4/4**;
  build and `test:pack` passed (12 packages, 28 explicit imports, 68 wildcard
  exports, bins); and `git diff --check` passed. These are dirty isolated-tree
  checks, not evidence for the actual `feat/0.3.0` checkout, a clean candidate,
  SCRATCH manifest, or remote CI.
- **Separate Pi proof of the remaining dual path.** `pi-taskflow` declares only
  `taskflow-core` as a workspace dependency, imports `executeTaskflow`/
  `saveRun`, and directly calls that runtime with a Pi runner and UI approval
  callback. A source inventory finds no `taskflow-control`, `ControlHost`, or
  `ControlStore` use in the Pi package. Thus Pi is not merely missing a test:
  it has no dependency edge through which it could share daemon/control
  authority. It remains **FAIL** for the one-ledger mandate.
- **Hard lower bound and successor.** The actual dirty `feat/0.3.0` branch
  still contains the pre-R83 MCP background source, so neither that branch nor
  Pi can be promoted. A real successor must make a daemon/control client own
  background admission and durable `runId`/`commandId`/provider route, then
  route background status/wait/cancel/approval through the same project journal
  and Receipt. Pi must either use that same client/ControlHost contract or fail
  closed by default until it does; a UI modal or `.pi` state cannot substitute.
  Required E2E spans real MCP stdio, Pi, daemon/UDS, CLI, restart, authority
  loss, and cross-client visibility. No file was staged, committed, pushed,
  tagged, published, or sent to remote CI.

---

### Round 84 — a daemon background route needs a durable executor bridge, not an un-awaited UDS call

- **Exact current topology.** In the isolated target tree,
  `tryControlPlaneRun` creates a new standalone `ControlHost` with
  `skipSingleton: true` and an MCP-process-local `llmProvider`. Conversely,
  `taskflowd` mounts hosts without an LLM provider, while its UDS capability
  list contains only `status`, `wait`, `admit`, `cancel`, and `approval`; its
  `admit` handler awaits the whole `host.admitAndRun(...)` call. That call
  mints the `runId` inside the durable admission saga and then awaits the phase
  scheduler. `ControlHost.wait()` currently returns one current snapshot rather
  than waiting for a terminal transition. These are source facts, not a
  background implementation.
- **Why a superficial wrapper is invalid.** `void host.admitAndRun(...)`, an
  invented client-side run ID, or returning from UDS before a durable admission
  boundary would lose command/run identity or conceal the
  `SlotCommitted → first DispatchIntent` recovery gap. Keeping the MCP runner
  as an in-memory callback after the client returns is equally invalid: the
  daemon has no durable, fenced, authenticated route to resume/observe/cancel
  that host-provided LLM process after disconnect or restart. It would merely
  rename a detached worker without creating one authority.
- **Narrow checks.** The target's strict migration classifier tests pass
  **18/18**, including malformed witness containers, residual witnesses,
  C3-pending-ref substitution, and complete active-witness coverage. That
  pure classifier still has no production authority caller and does not solve
  this executor/lifecycle boundary. The Round-84 topology conclusion is static
  source evidence only; it is not a new local/packed/remote lifecycle proof.
- **Required successor contract.** Before enabling any default background
  request, define and test a versioned `admit-start` result emitted only after
  one fenced durable command/admission/run record exists; it must include the
  exact project/domain/run/command IDs, admission generation, current
  `runVersion`, and an explicit non-terminal disposition. Define a real
  cancellable wait/poll/stream contract rather than calling a snapshot `wait`.
  Then provide either daemon-native durable providers or an authenticated
  long-lived host-executor protocol with provider identity, handle ownership,
  fence/incarnation, idempotent submit, acknowledgement, reconcile, cancel,
  disconnect, and restart semantics. The daemon must reject every phase and
  execution capability it cannot actually own; it may not accept a public DSL
  program and silently turn it into an LLM/script placeholder.
- **Unchanged GA decision.** This establishes that the next D18/D21 slice is a
  control/client/provider protocol and recovery design, not a one-line MCP
  routing change. MCP foreground remains a standalone partial path; MCP
  background, Pi, real cross-client wait, agent-provider continuity, and the
  P16 first-dispatch owner remain **FAIL**. No matrix row is promoted, and no
  SCRATCH evidence manifest, stage, commit, push, tag, publish, or remote CI
  action occurred.

---

### Round 85 — current `feat/0.3.0` repeatability packet preserves failure and reconfirms public P13 bypasses

- **Exact worktree and evidence.** This round is the actual dirty
  `/Users/beta/work/personal-projects/pi-taskflow` worktree on `feat/0.3.0 @
  e11b82109e04f69e56a37aceb3f78934f822ad14`, not any isolated capability or
  migration tree. Its raw evidence is under `.scratch/logs/round61-*` and its
  full narrative is in `.scratch/ga-evidence-report.md` Round 61.
- **Retained failure, not a rewritten history.** The first current owning
  `pnpm run test:control` capture was **217/218**, exit 1. The complete TAP
  identifies a ten-second crash-fixture child-barrier timeout at
  `recovery-owner.test.ts`; the predecessor fixture did not retain child
  stdout/stderr/exit diagnostics. The raw failing TAP and its SHA-256 remain
  in the Round-61 packet. The test-only harness now reports those diagnostics
  and uses a 30-second deadline; it changes no runtime authority or provider
  behavior. Post-change focused recovery is 3/3, `test:control` is 218/218,
  `test:daemon` is 16/16, `test:cli` is 5/5, and three serial `pnpm test`
  executions are 2281/2281. Later green results do **not** erase the first
  failure; they only improve the current local evidence record.
- **Fresh direct negative proof.** A temporary-home program imported the
  public current APIs and obtained
  `{"rawReserveEpoch":0,"coordinatorStateCreated":true,"copiedLockReleased":true}`:
  `openUserCoordinatorStore(env).reserve({ coordinatorEpoch: 0 })` created a
  user-global coordinator state without a singleton capability, and
  `releaseSingleton(JSON.parse(readFileSync(singletonLockPath(env))))` removed
  the active lock. The fixture deletes its temporary home in `finally`.
- **No status change.** The public authority bypasses directly sustain P13/P16
  failure on this branch; test-harness timing work cannot substitute for an
  opaque holder capability, an OS-backed replacement protocol, P14 freshness,
  C3 FirstDispatchOwner, provider contract, or one-ledger entrypoint work.
  Therefore §23.2 remains **PARTIAL**; P13/P14/P15/P16 and dependent rows
  remain **FAIL**. No clean candidate, package proof, push, tag, publish,
  release, or remote CI result exists.

---

### Round 86 — main-tree MCP defaults reject the detached 0.2 lifecycle; no D18/D21/P-row promotion

- **Current-source correction, not an isolated-tree claim.** The actual dirty
  `feat/0.3.0 @ e11b82109e04f69e56a37aceb3f78934f822ad14` MCP server now
  checks the exact explicit opt-out (`0|false|off|no`) before any background
  legacy state path. With the default-on control plane, `taskflow_run` in
  background mode returns a stable no-fallthrough error before settings/agent
  discovery, RunState creation, detached spawn, or `.pi` write. `taskflow_runs`
  denies `list/status/wait/cancel` before refresh/list/cancel can mutate an
  existing legacy tree, and `taskflow_resume` denies before reading or forking
  the old run history. The legacy compatibility suite now declares the opt-out
  rather than representing it as the product default.
- **Adversarial evidence.** The permanent default-on test confirms no `.pi`
  directory after a background request, byte-identical pre-existing `.pi`
  state after all four lifecycle actions and resume, denial for unset/empty/
  `1`/`true`/unexpected values, and all four documented escape values
  (`0`/`false`/`off`/`no`). The legacy suite also passed 14/14. Focused results are 5/5
  default-denial, 14/14 explicit legacy, and 2/2 legacy resume; the post-fix
  full suite is 2286/2286, control 218/218, daemon 16/16, and CLI 5/5. The
  previous 4-failure full TAP is retained in the Round-62 SCRATCH packet rather
  than overwritten by this green rerun.
- **Why the status remains FAIL/PARTIAL.** A default rejection is not a
  background lifecycle: it does not durably create a daemon-owned command,
  admission, run, provider route, Receipt, terminal wait/stream, cancel,
  approval, restart/reconcile, or cross-client visibility. MCP foreground
  remains a standalone `skipSingleton` path; the explicit opt-out still owns an
  independent `.pi` history; Pi has not gained a ControlClient/ControlStore
  edge. Thus D18 remains **PARTIAL**, D21 remains **PARTIAL**, the MCP
  run/status/wait/cancel/approval adversarial row remains **FAIL**, and
  P13/P14/P15/P16 remain **FAIL**.
- **Required successor.** Keep the denial tests as permanent pre-side-effect
  red contracts. Do not re-enable background until the Round-84 versioned
  daemon/executor protocol has a durable `admit-start` record, real provider
  ownership/recovery, and process E2E across MCP/Pi/CLI/UDS for the same
  run/command/approval and Receipt. No row promotion, clean candidate, pack
  proof, push, tag, publish, release, or remote-CI result exists in this round.

---

### Round 87 — main-tree Pi defaults reject every legacy execution affordance; still no client/lifecycle

- **Current-source correction.** The actual dirty
  `feat/0.3.0 @ e11b82109e04f69e56a37aceb3f78934f822ad14` Pi delivery adapter
  now mirrors the MCP boundary without taking a new `taskflow-control`
  dependency. With the control plane enabled by default, tool
  `action=run`, `action=resume`, live `action=recompute` (`dryRun:false`), and
  detached execution reject before `makeRunState`, legacy run reads/forks,
  agent/settings discovery, `executeTaskflow`, or `saveRun`. `/tf run`,
  `/tf resume`, `/tf recompute --apply`, dynamically saved flow shortcuts, and
  the interactive run-history resume affordance likewise stop before prompting
  the model or selecting the 0.2 path. Only the documented case-insensitive
  values whose lower-cased form is `TASKFLOW_CONTROL_PLANE=0|false|off|no`
  re-enable the legacy direct runtime;
  it is labelled emergency/non-GA. Offline dry-run/replay-style observation is
  not claimed as a shared lifecycle.
- **Adversarial proof.** `control-plane-gate.test.ts` verifies all documented
  disable values versus unset/empty/`1`/`true`/unexpected values; no `.pi`
  tree after each default direct execution attempt; byte-identical existing
  legacy bytes after default resume and live recompute; no default slash,
  saved-shortcut, or run-history prompt; and one explicit legacy script route.
  The test does not assert that a default request successfully runs: rejection
  is the intended and only proven behavior. The owning Pi suite remains local
  evidence, not an E2E daemon/client proof.
- **Why nothing is promoted.** Pi still has no ControlClient dependency edge,
  daemon-owned executor, durable common command/admission/run relation,
  provider-handle/Receipt chain, shared approval source of truth, terminal
  wait, restart/reconcile, or cross-client status/cancel behavior. The opt-out
  still writes an independent `.pi` RunState. Therefore D18 and D21 remain
  **PARTIAL**; the public lifecycle, P13, P14, P15, and P16 remain **FAIL**.
  This is a safety stop, not a Pi implementation.
- **Required successor.** Preserve these no-side-effect tests while completing
  Round 84's versioned `admit-start` plus durable executor contract. Then move
  Pi to a thin client of the same daemon command/run/approval journal as
  MCP/CLI/UDS, or keep it fail-closed. Before any status change, prove real
  process MCP+Pi+CLI/UDS cross-client execution, wait/cancel/approval,
  daemon/executor loss and restart, stale-writer rejection, provider recovery,
  all supported phase semantics, and Receipt identity. No clean candidate,
  stage, commit, push, tag, publish, release, or remote-CI result exists here.

---

### Round 88 — P14 scope freeze plus main-tree singleton/coordinator capability gate

- **P14 product scope decision.** On 2026-07-27 the product owner explicitly
  removed only one guarantee from the 0.3 GA contract: detection of a coherent
  rollback that restores every locally consulted authority byte, including the
  project ControlStore, root anchor, and coordinator authority, to one older
  valid snapshot. The files-only engine is now specified as
  trusted-local-disk and makes no anti-rollback promise for that event. No
  external monotonic witness or externally authenticated repair service is a
  0.3 gate. The permanent whole-root counterexample remains required negative
  evidence so later wording cannot imply a guarantee that does not exist.
  Partial loss, corruption, half-commit, sequence rollback, recovery/mutation
  races, stale owners, fencing, path replacement, compaction, and crash
  boundaries remain in scope. P14 stays **FAIL** for those remaining cells.
- **Retained red authority evidence.** On the actual dirty
  `feat/0.3.0 @ e11b82109e04f69e56a37aceb3f78934f822ad14` tree, the initial
  focused `stale-lock.test.ts` run passed 10/12 and failed exactly two new
  adversarial assertions: deserializing the public singleton owner JSON and
  passing it to `releaseSingleton()` deleted the live owner without error; and
  `openUserCoordinatorStore(env).reserve({coordinatorEpoch:0})` initialized
  global state/anchor without a live singleton authority. The raw failing TAP
  is retained rather than replaced by the green rerun.
- **Minimal authority correction.** A writer acquisition now mints an opaque
  process-held `SingletonMutationAuthority`; runtime authority is private
  `WeakMap` membership bound to the exact durable lock record. Singleton
  release, daemon embedded-host fences, and global coordinator mutations use
  that capability. A copied JSON record or caller-created object cannot act as
  it. Normal global coordinator mutation without a capability throws
  `TF_AUTHORITY_REVOKED` before state/anchor creation. A `baseDir` resolving to
  the canonical global coordinator is rejected instead of selecting the
  project-local epoch-`0` route. Reservation epoch is derived under the live
  capability fence; only project-local standalone and explicitly named
  non-GA fixture plumbing retain epoch `0`.
- **Current dirty-tree evidence.** The final focused authority suite is 13/13;
  focused store recovery is 47/47; `pnpm run test:control` is 221/221;
  `pnpm run test:daemon` is 16/16; `pnpm run test:cli` is 5/5; `pnpm test` is
  2295/2295; `pnpm run typecheck` and `git diff --check` exit 0. The first full
  control rerun is also retained: it passed 218/220 and failed only two stale
  regex expectations after the deliberate reserve-API change; the owning
  focused rerun and final full control run then passed. These are current
  shared-dirty-worktree results, not an isolated clean commit or remote
  candidate.
- **No matrix promotion.** This removes the concrete Round-80 copied-release,
  raw-global-write, caller-epoch, and canonical-alias bypasses. It does not
  provide stale-owner replacement, holder-authenticated UDS, Windows parity,
  P16 cross-store commitment/FirstDispatchOwner, capability-bound release,
  provider-independent recovery, pressure/backpressure, or entrypoint
  lifecycle closure. Therefore §23.2 remains **PARTIAL** and P13/P14/P16 remain
  **FAIL**. No stage, commit, push, tag, publish, release, or remote-CI action
  occurred.

---

### Round 89 — P16 first-dispatch lifecycle red contract and isolated production gate

- **P14 ordering correction.** The 2026-07-27 trusted-local-disk decision is
  final for 0.3: older Round text that requires an external monotonic witness,
  non-rollbackable root, or externally authenticated repair before P16 work is
  historical and superseded. Coherent restore of every locally consulted
  authority byte is outside the 0.3 guarantee. Partial/mixed rollback,
  corruption/loss, half-commit, crash/recovery races, stale authority,
  fencing, path replacement, compaction, and one-sided P/C failures remain
  blockers.
- **Current main-tree fact.** The shared dirty
  `feat/0.3.0 @ e11b82109e04f69e56a37aceb3f78934f822ad14` tree now has a pure
  E−1/E0 first-attempt tuple linker/validator. It rejects unsafe identities,
  dynamic/multiple provider shapes, forged route capabilities, and
  self-consistent tuples that disagree with independently supplied durable
  evidence. Its factory only registers a process-local function that declares
  the future `getOrCreate` contract; it does **not** prove provider atomicity,
  trusted registry ownership, or a durable Script implementation. Review also
  found that a caller can mutate a `BoundPlan` while retaining a syntactically
  valid old `boundPlanHash`; E−1 does not yet re-establish canonical plan
  integrity. Schema-1 persistence strips the in-memory `providerClass`, so the
  helper cannot silently change the P1 wire. It is not called by production
  admission, emits no V2 event, and creates no owner.
- **Permanent positive red contract.** In isolated worktree
  `codex/p16-g1-v2-admission @ e11b82109e04f69e56a37aceb3f78934f822ad14`,
  `p16-v2-first-dispatch-lifecycle.test.ts` requires a successful production
  call to use project schema 2, coordinator schema 3, unique events with
  selected identity/hash/revision cross-link assertions, an atomic E0
  `AdmissionIntentRecordedV2 + DispatchAttemptPlanned` batch, an atomic E4
  `CoordinatorCommittedObserved + SlotCommitted + RunAdmitted +
  FirstDispatchOwnerClaimed` batch, and ordered E5/E6 intent/submit before
  acknowledgement plus journal-event cross-links and a non-empty handle
  assertion. P2/C3 strict validators, hash recomputation, authoritative C3
  record lookup, and durable provider-job exact-tuple readback remain future
  acceptance work. The
  strengthened raw TAP remains red because production returns
  `TF_FEATURE_REQUIRED/none/none`. That failure is the truthful unimplemented
  lifecycle, not a test to weaken.
- **Isolated safety slice only.** The same isolated tree now rejects a normal
  singleton writer, taskflowd's real parent-authorized `skipSingleton` mount,
  and explicit standalone legacy disclosure before P1/C2 admission or
  ownerless replay. The stop is an explicit protocol-readiness predicate, not
  `schemaVersion < 2`: a header bump cannot fall through into the legacy body,
  and future P2+C3-active admission must dispatch to a separate exhaustive V2
  route. Focused ControlHost gates are 3/3; a real taskflowd + UDS
  gate is 1/1; isolated typecheck exits 0. Checked command/journal/reservation/
  provider-marker state is unchanged after authority bootstrap. This is not a
  proof that bootstrap itself created no P1/C2 bytes, nor a generic provider
  call-count proof. Pi still has direct `executeTaskflow` ingress outside
  ControlHost, so this is not a product-wide admission stop. An earlier gate
  that exempted `skipSingleton` was rejected
  by independent review because real taskflowd uses that topology; its result
  is historical and receives no safety credit. The broad legacy success suite
  now intentionally conflicts with the corrected stop, so the previous
  277/287 count describes only that rejected intermediate gate, not current
  source. The corrected gate is not transplanted to main.
- **Main-tree local evidence.** Focused first-dispatch/admission/schema
  coverage is 24/24; `pnpm run typecheck`, `test:control` 236/236,
  `test:daemon` 16/16, `test:cli` 5/5, and `pnpm test` 2310/2310 all exit 0.
  These results describe the current dirty P1/C2 main tree only. They neither
  satisfy the isolated positive lifecycle contract nor promote any GA row.
- **Status and next gate.** P16 remains **FAIL**; D16/D24/D30/D37 and dependent
  rows remain unchanged. P16-1R is still a **proposed**, independently reviewed
  contract, not an accepted wire decision. Before implementing P2/C3, record
  explicit acceptance with owner/date/revision and schema-freeze boundary, or
  keep all work exploratory. After acceptance, first repair canonical
  BoundPlan proof and the real provider capability boundary, then implement
  fresh P2/C3 E0–E4 with strict migration/quarantine and immutable cross-store
  evidence. Only a strict positive lifecycle plus crash/stale-writer review can
  justify considering the gate for main-tree transplant.

---

### Round 90 — hostile-object-safe E−1 snapshot and planned-event lineage

- **P14 scope is unchanged.** The coherent rollback of every locally consulted
  authority byte to one older internally valid snapshot remains outside the
  0.3 guarantee. This round neither adds an external witness nor uses that
  exclusion to waive partial/mixed rollback, corruption, half-commit, crash,
  stale-authority, fencing, replacement, compaction, or one-sided P/C tests.
- **Two first failures are permanent evidence.** The first focused red was
  0/2: a changed `BoundPlan.program` retained its old identity and a valid
  replacement `plannedEventId` was not compared with the persisted tuple. An
  independent reviewer then constructed a stronger TOCTOU: a `Proxy` could
  expose benign bytes during fingerprinting and an evil command during
  extraction, while non-enumerable execution fields escaped canonical JSON.
  The follow-up hostile-object red was 0/4.
- **Exploratory E−1 correction in the isolated tree.** `linkProgram()` now
  creates and owns a strict plain-JSON snapshot before validation, hashing, or
  later use. It rejects Proxy, accessor, non-enumerable, symbol, cyclic,
  sparse-array, class/non-JSON, and non-finite inputs; recursively freezes the
  program and grant references; freezes the `BoundPlan`; and retains exact
  process-local `WeakMap` provenance. A caller-owned object is never reread
  after Link. This intentionally tightens the programmatic API: explicit
  `undefined`, class instances, and cross-realm objects are outside the
  accepted plain-JSON boundary.
- **Event lineage is now internally coherent, not durable authority.**
  `plannedEventId` is allocated in the E−1 context, persisted in the strict
  tuple, included in `plannedAttemptHash`, and compared against independent
  durable evidence. It deliberately does not enter `requestDigest` or the
  provider idempotency key because it identifies the future journal envelope,
  not a different logical provider effect. The current helper cannot prove
  that a caller actually read independent P2/C3 records; that remains a
  production-reader obligation.
- **Independent re-review and evidence.** After the reviewer-reported Proxy and
  non-enumerable bypasses were fixed, re-review found no remaining safety
  blocker in this seam; the remaining API type mismatch was corrected by
  declaring frozen `grantRefs` readonly. Current isolated results are:
  first-dispatch 40/40, typecheck exit 0, ControlHost stop 3/3, real
  taskflowd+UDS stop 1/1, and `git diff --check` exit 0. The positive P2/C3
  lifecycle remains the required 0/1 red. The broad legacy suite is
  intentionally 199 passed, 95 failed, 6 cancelled, 300 total because the
  corrected production stop invalidates legacy success fixtures. The separate
  dirty main P1/C2 tree was rerun unchanged: typecheck exit 0,
  `test:control` 236/236, `test:daemon` 16/16, `test:cli` 5/5, and
  `pnpm test` 2310/2310. Those green main results do not satisfy the isolated
  positive lifecycle.
- **No promotion.** This is a reviewed E−1 safety seam in
  `codex/p16-g1-v2-admission`, not a main-tree transplant, accepted P16-1R
  wire, trusted provider registry, atomic durable `getOrCreate`, P2/C3
  implementation, all-entrypoint stop, or GA proof. P13/P14/P15/P16 and all
  dependent rows retain their prior FAIL/PARTIAL status. No stage, commit,
  push, PR, tag, publish, release, or remote-CI action occurred.

---

### Round 91 — singleton release inode retirement and exact idempotency proof

- **Scope and worktree.** P16-1R remains an unaccepted proposed wire, so this
  round did not invent P2/C3. It used the existing accumulated isolated
  `codex/p16-g1-v2-admission @
  e11b82109e04f69e56a37aceb3f78934f822ad14` worktree for a P13 predecessor
  slice. This is not a clean dedicated P13 candidate and none of its code is
  attributed to the separate dirty main worktree.
- **Permanent first failures.** Five observed behaviors are retained as raw
  reds: release could delete a replacement inserted after validation; a
  replacement moved by retirement was left off the public pathname when the
  following directory fsync failed; an already-visible replacement made
  release silently return; a missing public owner without proof was silently
  accepted; and a second complete but conflicting same-epoch retirement proof
  was ignored. These are behavior counterexamples, not inferred risks.
- **Exploratory correction.** Release now hard-links the observed owner into a
  private `0700` retirement directory, validates it through an
  `O_NOFOLLOW` descriptor, renames the public pathname into retained
  `owner.json`, and compares the moved inode with the claim. Any post-rename
  fsync/lstat/mismatch failure performs a no-overwrite hard-link restore and
  parent fsync before returning a typed durability failure. A different owner
  visible at entry raises `TF_AUTHORITY_REVOKED`. A missing public owner is
  idempotent only when one and only one real same-epoch retirement directory
  contains regular `claim.json`/`owner.json` files that are the same inode and
  exact-match the process capability; conflicting, duplicate, file, symlink,
  malformed, or non-matching proof state fails closed.
- **Review and evidence.** Independent review first rejected the post-rename
  error path and silent/idempotency ambiguities, then found the conflicting
  proof bypass. After the added reds and corrections, the reviewer reported
  no remaining blocker in this local slice and independently reran 9/9
  targeted tests plus `git diff --check`, both exit 0. Current recorded
  results are focused release 9/9, typecheck exit 0, and the complete isolated
  `stale-lock.test.ts` 21/22. Its only failure is an existing conflict between
  the exploratory P16 admission stop (`TF_FEATURE_REQUIRED`) and the older
  host-fence assertion (`TF_AUTHORITY_REVOKED`); it is reported, not weakened.
- **No P13 promotion.** The singleton release still relies on
  `withExclusiveLockFile` for `.authority`, whose permanent P13
  pathname-release counterexample proves that the old holder can delete a
  replacement transition lock. Retirement storage also grows without a safe
  bounded cleanup protocol. Real multi-process replacement, restore
  EEXIST/failure, persistent I/O faults, crash/reopen cuts, pressure,
  parent-directory replacement/openat semantics, Windows, and non-local
  filesystems are unproven. P13 therefore remains **FAIL** in the GA matrix
  (the isolated release sub-slice is only locally reviewed), and all dependent
  rows remain unchanged. No stage, commit, push, PR, tag, publish, release, or
  remote-CI action occurred.

---

### Round 92 — the generic transition lock is capability-bound, but not bounded or recoverable

- **Scope and preserved threat boundary.** Work stayed in the accumulated
  isolated `codex/p16-g1-v2-admission @
  e11b82109e04f69e56a37aceb3f78934f822ad14` tree. The 0.3 P14 exclusion
  remains only coherent rollback of every locally consulted authority byte to
  one older internally valid snapshot. This round did not add an external
  witness and did not waive partial/mixed rollback, corruption, half-commit,
  crash, stale authority, path replacement, compaction, or one-sided P/C
  failures.
- **Permanent first failures.** The old generic `withExclusiveLockFile`
  release returned success after deleting a replacement lock. Review-driven
  reds then proved four more gaps: a pending transition published between the
  initial scan and `mkdir` admitted the body; lock-parent fsync failure still
  reached owner publication/body; a body failure masked a simultaneous
  release failure; and pending→complete fsync failure could leave no pending
  barrier. A final red copied the exact public `lockId` and bytes into a
  different inode before claim; release accepted and retired that replacement.
  All three raw red runs are retained.
- **Exploratory correction.** Each acquisition attempt now has a fresh random
  `lockId`; owner publication is `wx`, file-fsynced, directory-fsynced, and
  recorded as exact bytes plus acquisition `dev`/`ino`. Release publishes a
  durable `0700` pending transition, hard-links and fd-validates the owner,
  requires exact acquisition identity, moves the whole public lock directory,
  verifies the moved inode, and commits pending→complete only after parent
  fsync. Replacement and uncertain commit paths restore or retain a pending
  poison barrier and fail typed. A pending appearing scan→mkdir makes the
  contender retire only its own capability-bound directory and retry; it
  never enters the body. Body and release failures are both preserved.
  Completed artifacts are deliberately retained rather than recursively
  pathname-deleted.
- **Frozen-source evidence.** The final `paths.ts` SHA-256 is
  `0a7f027bcafff3824599d4efdaf7b7ee58ad76b0b2044d92a0a675e4ef51298c`.
  On that source, `paths-hardening.test.ts` is 22/22, typecheck and
  `git diff --check` exit 0, the existing 20+ process singleton case passes,
  and the real child-process ProjectStore recovery waits for the same commit
  lock. A frozen 100-round × 32-process run completed 3,200 fresh singleton
  acquisitions with exactly one holder/epoch per round in 230,009 ms. Its
  explicit scope is fresh-singleton exclusion only; it is not stale
  replacement, crash recovery, or performance-bound proof.
- **Broad incompatibility is visible.** The two owning files are 43/44; the
  only failure is the accumulated P16 production stop returning
  `TF_FEATURE_REQUIRED` before an older test expects
  `TF_AUTHORITY_REVOKED`. The full isolated control suite is 214 pass, 95
  fail, 6 cancelled, 315 total. Most failures are legacy lifecycle fixtures
  stopped before admission by that P16 gate; they are not hidden or converted
  into greens, and the broad red means this tree is not a candidate.
- **Independent review and no promotion.** Review found no remaining blocker
  in the named cooperative/local-POSIX replacement slice, but found three
  binding P13 gaps: every successful lock operation retains another completed
  tree and each acquisition scans the growing parent; pending transitions have
  no unique recovery/repair owner; and the protocol does not hold a
  directory-fd/openat-style capability across the body, so parent replacement
  and same-inode post-read mutation are outside the proof. First-parent
  durable creation, SIGKILL crash cells, bounded retention, Windows, and
  non-local filesystem semantics also remain open. Consequently P13, P14,
  P15, P16, and all dependent rows keep their prior **FAIL/PARTIAL** status.
  No stage, commit, push, PR, tag, publish, release, or remote-CI action
  occurred.

---

### Round 93 — mutable stores now require an owner capability by default; the product writer path is still not closed

- **Scope and product boundary.** Work remained in the accumulated isolated
  `codex/p16-g1-v2-admission @
  e11b82109e04f69e56a37aceb3f78934f822ad14` implementation tree. The main
  `feat/0.3.0` tree received documentation/evidence only. The P14 decision is
  unchanged: coherent rollback of every locally consulted authority byte to
  one older internally valid snapshot is outside the 0.3 anti-rollback
  guarantee. Partial/mixed rollback, corruption, half-commit, crash,
  stale-owner, fencing, path replacement, compaction, and one-sided P/C
  failure remain mandatory.
- **Permanent initial failures.** A raw mutable
  `openProjectControlStore(projectRoot)` initialized project authority and a
  raw mutable `openControlRegistry(env)` initialized user-global discovery
  without any singleton owner capability. Both first failures are retained.
- **Exploratory correction.** Mutable ProjectStore and Registry opens now
  require the process-local singleton capability minted through the private
  WeakMap, or a separately named explicit non-GA escape. A callback-only fence,
  a structural `{}` capability, a stale capability, and a capability bound to
  another `TASKFLOW_HOME` cannot authorize or initialize state. The real
  capability covers open/recovery and every public mutation. Read-only
  ProjectStore handles centrally reject mutators. `skipSingleton` changes
  topology only: auto/coordinated mode requires a parent capability unless a
  caller explicitly enters non-GA mode. A stale writer is rejected before the
  P16 feature gate. Failed post-acquisition Host initialization retires the
  owner it just published so later callers cannot attach to a ghost writer.
- **Review-driven correctness fixes.** Initial independent review found a
  synchronous self-deadlock: provider submission held the singleton authority
  and commit lock, then a nested journal read reacquired the same authority
  lock. A store-local synchronous depth now permits only nested calls already
  inside the verified outer capability scope. Review also found and closed
  read-only mutation, auto+skip authority, cross-home pre-rejection writes,
  P16 error-code masking, and startup owner leakage. Final integrity review
  reran 12/12 focused cases and `git diff --check` and reported no remaining
  Blocker/Major in this named local authority-correctness slice.
- **Frozen evidence.** Final `stale-lock.test.ts` is 27/27; the unchanged
  Round92 path hardening suite is 22/22; typecheck and diff-check exit 0. The
  broad isolated control suite is 220 pass, 94 fail, 6 cancelled, 320 total,
  exit 1. A focused daemon authority run is 3 pass / 2 fail / 5 total: startup
  cleanup, concurrent writer exclusion, and the production P16 stop pass;
  two older late-fault tests expect a lifecycle path that the P16 stop never
  enters. These reds remain candidate evidence, not ignored noise.
- **No matrix promotion.** Quality review identified binding product gaps:
  default MCP foreground still creates `standalone-local` instead of routing
  through ControlClient/UDS to the daemon writer; registry corruption and I/O
  errors are still collapsed into empty discovery; the explicit non-GA
  boolean and raw stores remain public same-process surfaces; and optional
  mutable-open/compaction signatures now fail old calls only at runtime.
  Round92's unbounded transition artifacts, unrecoverable pending state, and
  missing fd-relative authority also remain. P13, P14, P15, P16 and all
  dependent rows therefore keep their prior **FAIL/PARTIAL** status. P16-1R
  remains a reviewed proposal, not accepted wire. No stage, commit, push, PR,
  tag, publish, release, or remote-CI action occurred.

---

## Wire freeze / release

| Item | Status |
|------|--------|
| Wire freeze | **Not approved** (RFC §24) |
| Version align all packages → 0.3.0 | **BLOCKED** until behavior true |
| Remote CI green for candidate SHA | **N/A** (no push authorization) |
| Closed-loop GA claim | **FORBIDDEN** until all MUST PASS + remote CI + clean tree |
