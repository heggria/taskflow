# 0.3.0 Closed-Loop GA Traceability Matrix

> Living matrix: **full** §23 + D1–D38 + P1–P16. No “selected MUST” shortcuts for GA claims.  
> **Every PASS must link a behavior test or runtime proof.** PARTIAL/FAIL stay explicit.  
> Branch: `feat/0.3.0`. Tip evidence SHA is stamped in `{SCRATCH}/ga-evidence-report.md`.  
> Do **not** claim closed-loop GA while any MUST row is FAIL/PARTIAL, remote CI is unproven, or dual paths remain.

**Legend:** PASS | PARTIAL | FAIL | N/A | DEFER

**Evidence root:** plan `{SCRATCH}` = implementer scratch (logs + sha256 in `ga-evidence-report.md`).

---

## Dual-path inventory (freeze)

| Surface | Path today | ControlStore? | Status |
|---------|------------|---------------|--------|
| MCP foreground `taskflow_run` | `tryControlPlaneRun` → ControlHost `schedulePhases` + script/llm providers | Yes (standalone host, **skipSingleton**) | **PARTIAL** — not daemon singleton writer |
| MCP background | 0.2 `launchMcpBackgroundRun` / detached | No (`.pi` store) | **FAIL** vs one-ledger mandate |
| MCP when `TASKFLOW_CONTROL_PLANE=0` | `executeTaskflow` 0.2 | No | **intentional opt-out** (PARTIAL for GA) |
| Pi adapter run | `executeTaskflow` in `pi-taskflow/src/index.ts` | No | **FAIL** |
| CLI standalone / writer | `bootstrapControl` → local ControlHost | Yes | **PASS** (local) |
| CLI attach (auto) | `controlClientRpc` UDS → daemon writer | Yes (via daemon) | **PASS** (local; `uds-hardening`) |
| taskflowd | singleton + UDS + ControlHost mounts | Yes | **PASS** (local) |
| approve() | CAS → terminal + Receipt without re-`schedulePhases` | Yes journal | **FAIL** mandate 6 lifecycle truth |

---

## §23 GA checklist (complete)

| # | Item | Impl | Behavior test / proof | Evidence | Status |
|---|------|------|----------------------|----------|--------|
| 23.1 | Fresh install, default auto, one run, no manual daemon config | `bootstrap.ts` controlMode auto | `closed-loop.test.ts`, cli tests | unit/control logs | **PARTIAL** — auto works; MCP still standalone skipSingleton |
| 23.2 | Concurrent client start → single writer / attach | `singleton.ts` | daemon concurrent start; modes stress | daemon tests | **PARTIAL** — not 32×100 rounds logged this tip |
| 23.3 | Stale socket recovery | uds-server unlink dead sock | daemon UDS tests | daemon tests | **PASS** (local) |
| 23.4 | Per-project store; registry multi-mount; standalone+daemon same store | project-store, registry | closed-loop + daemon mount | control/daemon | **PARTIAL** — same schema; MCP not always same writer |
| 23.5 | Registry wiped → reopen restores projectId/domainId from header | identity + header | closed-loop identity | control tests | **PASS** (local) |
| 23.6 | maxActiveRuns slots≡1; N+1 capacity | coordinator | closed-loop capacity | control | **PASS** (local) |
| 23.7 | committed/orphan-suspect release only D37 | coordinator normalRelease/forceRelease | closed-loop forceRelease | control | **PASS** (local) |
| 23.8 | reconcile exhausted → unknown + needs-operator + no Receipt | reconcile + control-host | closed-loop reconcile | control | **PASS** (local) |
| 23.9 | wait snapshot + TF_RECONCILE_REQUIRED | control-host.wait | closed-loop wait | control | **PASS** (local) |
| 23.10 | CoordinatorCommandRecord setMax/force | coordinator | closed-loop | control | **PASS** (local) |
| 23.11 | Approval parked releases slot; approve → queued+re-reserve; CAS | parkForApproval + exact continuation dispatcher + daemon startup recovery | approval-cas; approval-continuation five-window crash matrix; packaged WebUI | control/browser | **PASS (local)** for approve/reject — four pre-dispatch prefixes recover exactly once; post-dispatch ambiguity never replays; provider outcome controls Receipt |
| 23.12 | Command idempotency; disclosure re-auth; ArtifactRef authz | claimCommand; artifact-uri | modes-and-stress; p9 | control | **PARTIAL** |
| 23.13 | Receipt event manifest; assurance when used | issueReceipt | p9 D21 route; ga-samples | SCRATCH samples | **PARTIAL** — no hardcoded artifactIntegrity ok; provenance incomplete |
| 23.14 | No DomainTransfer; no federated multi-store | surface | p-adr inventory | p-adr test | **PASS** |
| 23.15 | Public-surface goldens; cancelled; parked stage | types + goldens | wire-types; public surface | unit | **PARTIAL** — D21 dual path |
| 23.16 | P1–P16 ADRs present for shipped wire | docs/internal/p-adrs | p-adr-inventory.test.ts | control | **PASS** (docs present) |

---

> Beta.2 browser note: master RFC D13 and `rfc-0.3.0-beta.2-web-console.md`
> v7 place WebUI in 0.3.0-beta.2. P17 v5 remains provisional; its executable
> endpoint/projection/content/cursor foundation and all 29 WebGateway route
> slots now exist. Packaged multi-project browser E2E covers session exchange,
> SSE, polling fallback, durable cancel, Receipt/artifact rendering, checked
> Receipt JSON export, visibly separate why-stale/replay surfaces, and
> Receipt-bound offline replay with zero provider calls, zero durable writes and
> unchanged `nextCommitSeq`, plus 320px layout. The same replay proof passes
> Chromium 149.0.7827.55, Firefox 151.0 and WebKit 26.5. The 149 hash-bound
> reference renders remain `draft-unapproved`. The complete packaged path also
> passes native Google Chrome 150.0.7871.184; native Edge/Safari and AT review
> remain outstanding. The
> complete §15 conformance matrix and external comprehension/content review are
> still release gates separate from core P1–P16 closure.

## D1–D38 (complete)

| ID | Topic | Impl | Behavior test / proof | Status |
|----|-------|------|----------------------|--------|
| D1 | CACP | architecture | RFC + control packages | **PASS** (design shipped) |
| D2 | Single scheduler | phase-scheduler + residual 0.2 | phase-scheduler-adversarial; Pi still 0.2 | **PARTIAL** |
| D3 | Planes Intent…Ledger | linker + store + host | closed-loop | **PARTIAL** |
| D4 | Entity set | types.ts + stores | wire-types | **PARTIAL** — NodeInstance/Attempt not fully first-class on disk |
| D5 | controlMode auto/coord/standalone | bootstrap | closed-loop modes; coordinated fail-closed | **PARTIAL** — UDS attach incomplete for all clients |
| D6 | Scoped authority | project vs coordinator vs registry | closed-loop | **PASS** (local) |
| D7 | projectId + directoryBinding | identity.ts | p1-semantics identity | **PASS** (local) |
| D8 | Thin MCP + CLI | mcp-route, cli | mcp-bind; cli tests | **PARTIAL** — MCP skipSingleton |
| D9 | Async ExecutionProvider | script + host-llm | script-provider; host-llm; adversarial mixed | **PARTIAL** — 12 phase kinds incomplete |
| D10 | Policy deny/sub/attenuate | policy.ts | p1-semantics policy | **PASS** (local) |
| D11 | Unsupported sandbox fail-closed | grants/enforcement | p8 grants tests | **PASS** (local) |
| D12 | Migration tiered; no DomainTransfer | rollback tiers P10 | p10 tests | **PASS** (local design) |
| D13 | ControlHost + journal + BoundPlan + CLI | packages | closed-loop + cli | **PASS** (local) |
| D14 | Workspace capability | deferred product | — | **DEFER** (0.3.1 unless MUST) |
| D15 | OS principal; mcp:* weak | UDS principal checks | uds-hardening | **PARTIAL** |
| D16 | at-least-once + idempotent submit + reconcile | claimCommand + reconcile | modes stress; closed-loop | **PARTIAL** |
| D17 | BoundPlan + BoundFragment | linker; bound-fragment | bound-fragment tests | **PASS** (local) |
| D18 | One ControlHost semantics all modes | control-host + bootstrap | dual-path inventory | **PARTIAL** — MCP standalone skipSingleton; Pi absent |
| D19 | Grant refs revalidation | grants.ts | p8 | **PASS** (local) |
| D20 | Dual-write stop self; no kill foreign 0.2 | legacy-conflict | p9 | **PASS** (local) |
| D21 | Public 0.2.4 surface at GA | goldens + MCP route | host-entry; public surface; adversarial | **PARTIAL** — background/Pi dual |
| D22 | One journal per project domain | project-store | store-recovery | **PASS** (local) |
| D23 | Orthogonal enforcement | policy/grants | p8 | **PARTIAL** |
| D24 | CommandRecord same batch as events | store compareAndCommit | approval-cas | **PARTIAL** |
| D25 | boundFragmentHash + executionSemanticHash | bound-fragment; hash | bound-fragment tests | **PASS** (local) |
| D26 | ArtifactRef digests; SecretRef no content | artifact-uri | p9 | **PARTIAL** |
| D27 | No DomainTransfer | surface | inventory | **PASS** |
| D28 | Node≥22.19; pnpm 11; toolchain | package engines | env.txt | **PASS** (local env) |
| D29 | ControlRegistry multi-project | registry.ts | p1 registry lock | **PASS** (local) |
| D30 | maxActiveRuns slots≡1 capacity | coordinator | closed-loop capacity | **PASS** (local) |
| D31 | RunStatus vs RunStage; parked | types.ts | wire-types | **PASS** |
| D32 | Embedded multi-mount = same singleton as taskflowd | singleton + daemon | daemon concurrent | **PARTIAL** — MCP not on multi-mount writer |
| D33 | unknown non-terminal; reconcile budget | reconcile | closed-loop | **PASS** (local) |
| D34 | Approval durability modes | types + approval.ts | wire-types; approval-cas | **PARTIAL** |
| D35 | No federated multi-ControlStore | surface | inventory | **PASS** |
| D36 | reserved TTL; committed not TTL-only | coordinator | closed-loop | **PASS** (local) |
| D37 | normalRelease / forceRelease predicates | coordinator | closed-loop forceRelease | **PASS** (local) |
| D38 | park → release slot; approve → queued+re-reserve | parkForApproval; exact continuation dispatcher; startup handoff recovery | approval-cas; approval-continuation crash-prefix matrix; packaged WebUI | **PASS** (local) for plain approve/reject; edit remains separately gated |

---

## P1–P16 (complete)

| ID | Topic | Impl | Behavior test / proof | Status |
|----|-------|------|----------------------|--------|
| P1 | Policy compiler | policy.ts | p1-semantics | **PASS** (local) |
| P2 | Empty policy host-default exposure | policy.ts | p1-semantics | **PASS** (local) |
| P3 | Domain/registry identity clone/worktree | identity.ts | p1-semantics identity | **PASS** (local) |
| P4 | Negotiation / TF_* errors | types TF_ERROR_CODES; UDS hello | uds-hardening; closed-loop | **PARTIAL** |
| P5 | Public goldens matrix | core goldens + D21 | public-surface / host-entry | **PARTIAL** |
| P6 | BoundPlan hash covers grants/policy/timeout | hash.ts linker | p1-semantics hash | **PASS** (local) |
| P7 | BoundFragment dual hash | bound-fragment.ts | bound-fragment tests | **PASS** (local) |
| P8 | Grants / enforcement | grants.ts | p8 tests | **PASS** (local) |
| P9 | Legacy conflict + artifact URI | legacy-conflict; artifact-uri | p9 tests | **PASS** (local) |
| P10 | Rollback tiers | compaction/rollback | p10 | **PASS** (local) |
| P11 | Compaction cursor / TF_CURSOR_EXPIRED | compaction | p11 | **PASS** (local) |
| P12 | Command batch re-auth | CommandRecord | partial | **PARTIAL** |
| P13 | Bootstrap / singleton / platforms | bootstrap; singleton; UDS | daemon; uds-hardening | **PARTIAL** — Windows non-GA |
| P14 | ControlStore engine durability | project-store; writeFileAtomic parent fsync | store-recovery | **PARTIAL** — crash txn incomplete |
| P15 | Approval protocol | approval + host + private continuation checkpoint + startup handoff recovery | approval-cas; five-window approval-continuation crash matrix; HTTP and packaged browser approval | **PARTIAL** — plain approve/reject and restart safety PASS locally; durability-mode negotiation and edited-plan continuation remain |
| P16 | Coordinator capacity/D37 | coordinator | closed-loop | **PASS** (local) |

---

## Required adversarial tests (objective)

| Case | Test location | Status |
|------|---------------|--------|
| mixed agent+script both execute | `phase-scheduler-adversarial.test.ts` | **PASS** (local) |
| two dependent scripts; first marker | same | **PASS** (local) |
| upstream fail → no success Receipt | same | **PASS** (local) |
| MCP identity/store mismatch no marker | p9 legacy / identity | **PARTIAL** — TF_LEGACY_CONFLICT / identity; not full MCP mismatch e2e |
| daemon writer + CLI auto via UDS | `uds-hardening.test.ts` | **PASS** (local) |
| RPC before hello no side effect | `uds-hardening.test.ts` | **PASS** (local) |
| missing/unknown projectId denied | `uds-hardening.test.ts` | **PASS** (local) |
| MCP run/status/wait/cancel/approval same ledger | — | **FAIL** — background dual store; status tools still 0.2-heavy |
| 32-process stale takeover ×100 = one writer | partial MP tests | **PARTIAL** / not full 32×100 logged |
| old epoch writer mutation rejected | fencing tests | **PARTIAL** |
| cancel kills process tree; no grandchild marker | script-provider cancel | **PARTIAL** |
| ambiguous cancel nonterminal | reconcile paths | **PARTIAL** |
| approval cannot complete unless resumed provider work completes | approval-continuation + packaged browser approval | **PASS** (local) |
| capacity exceeded retry-same-command | closed-loop capacity | **PARTIAL** |
| corrupt header/journal/seq fail-closed | store-recovery | **PARTIAL** |
| Receipt verifier tamper / missing enforcement | issueReceipt honesty | **PARTIAL** — no full tamper suite |
| packed clean consumer daemon+CLI e2e | pack smoke | **PARTIAL** — 12-pkg smoke; not full packed daemon+CLI path proven every tip |

---

## Round notes (recent)

### Round 16 — phase scheduler + host LLM (`558f28ad`)
- `phase-scheduler.ts`, `host-llm-provider.ts`; admitAndRun per-phase; MCP fg injects LLM
- Adversarial multi-script / mixed / upstream fail
- unit **2131** then advanced

### Round 17 — ControlClient + UDS harden (`94d5b7b7`)
- control-client; CLI attach UDS; hello-before-RPC; exact projectId
- unit **2134**

### Round 18 — matrix complete rewrite + SCRATCH evidence package
- Full §23 + D1–D38 + P1–P16 (this file)
- Dual-path inventory explicit FAIL/PARTIAL
- SCRATCH logs + sha256 in `ga-evidence-report.md`
- **Verdict remains NOT closed-loop GA**

---

## Wire freeze / release

| Item | Status |
|------|--------|
| Wire freeze | **Not approved** (RFC §24) |
| Version align all packages → 0.3.0 | **BLOCKED** until behavior true |
| Remote CI green for candidate SHA | **N/A** (no push authorization) |
| Closed-loop GA claim | **FORBIDDEN** until all MUST PASS + remote CI + clean tree |
