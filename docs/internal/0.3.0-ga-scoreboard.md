# 0.3 Trusted Effects — GA Scoreboard

> Living ledger. **Do not claim GA** unless L6 is PASS with human tag evidence.

**Last updated:** 2026-08-10 (candidate `96a32e8`; RC pipeline evidence + exact-SHA store/typecheck re-verify)
**Branch:** `rc/0.3.0-trusted-effects` (tip now carries docs-only commits — scoreboard refresh `0834c59`, PR draft `7add160`, skills `59fd9c3` — on top of the code candidate; product code unchanged)
**Code candidate:** `96a32e8` (fix(store): harden project .pi discovery against tmp and home roots)
**Current evidence:** local RC pipeline green (logs `/tmp/taskflow-03-rc-final/`): full monorepo typecheck PASS; full unit suite 2202 tests → 2198 pass / 0 fail / 4 skipped; full build PASS; `test:pack` 9 packages PASS; built Codex MCP e2e 16/16; `pnpm audit --prod` clean. The pipeline logs were produced with HEAD at `6071fb2` (build-info stamp `6071fb2a3aed`); the `96a32e8` store-discovery hardening delta was re-verified at its exact SHA by focused store suite 69/69 + full typecheck (t_bf5ab0c1). These checks cover the post-review cache/composition, information-flow, malformed-admission, cleanup/lease, before-image GC, and store-discovery hardening fixes. Exact-SHA remote CI is **still open** — the branch **is pushed** to origin (via worker-mac SSH relay), but no CI run exists for it: `.github/workflows/ci.yml` triggers only on PR→main / push to main, and no open PR has head `96a32e8` (Draft PR #117 head is still the stale `8778b2f`); opening/updating that PR is the human gate. Live Codex CLI E2E has **not** been rerun for `96a32e8`; historical evidence only: Draft PR #117 CI run 31167592775 passed all 10 matrix jobs plus GitHub CodeQL on `1478510f`, and the prior candidate passed built Codex MCP 16/16 and a live Codex CLI A→B→C run.

---

## Acceptance Gate

| Level | Status | Evidence | Notes |
|-------|--------|----------|-------|
| L1 local | **PASS** | `96a32e8`: monorepo typecheck PASS + audit --prod clean (RC pipeline logs; store delta re-verified at exact SHA) | RC pipeline green; typecheck re-run at exact SHA `96a32e8` |
| L2 contract | **PASS** | `96a32e8`: full unit suite 2202 tests → 2198 pass / 0 fail / 4 skipped + build PASS + test:pack 9 pkgs + built Codex MCP e2e 16/16 | pipeline logs built at evidence SHA `6071fb2`; exact-SHA remote CI not yet evidenced — branch pushed to origin but no run exists (CI triggers only on PR→main / push main; no PR head = `96a32e8`) |
| L3 browser/electron | **N/A** | — | |
| L4 real-environment | **PASS (built-MCP fixture)** | built Codex MCP comprehensive e2e 16/16 incl. TE fixture — `fs.write` committed through resources + ledger-backed why-effect — against the built dist bin (`codex-mcp-full.log`; evidence SHA `6071fb2` per build-info stamp) | fixture/network-free runner on the real built artifact, **NOT** a live Codex CLI run: `test:e2e-codex` NOT rerun on `96a32e8` (prior-candidate A→B→C historical only) |
| L5 released | **FAIL** | no tag/publish | human gate |
| L6 ga | **FAIL** | L5 missing | **NOT GA** |

**Highest proven for the current code candidate:** **L4 real-environment (built-MCP fixture)** — the live-Codex-CLI leg of L4 and exact-SHA remote CI remain open.
**User-facing claim allowed:** *The post-review Trusted Effects candidate is green through the built-MCP real-environment fixture for admitted declared FS-write targets (RC pipeline at `96a32e8`: typecheck + 2202-test suite 2198/0/4 + build + pack + built Codex MCP 16/16 incl. TE fs.write + why-effect + audit clean; store delta re-verified at exact SHA); live Codex CLI run, exact-SHA remote CI, release, and GA gates remain open.*
**G5 status:** **contract pass for admitted declared targets** — PathRef escape, direct-write restore, crash recovery, multi-file rollback, terminal cleanup failure, activation/journal double-fault lease release, before-image GC, gate fast path, cross-run/dynamic-spawn cache bypass, and event-kernel fallback are adversarially covered. Resolve-only execution does not prevent or restore writes to undeclared paths; that requires a future FileBroker/native sandbox and is not a 0.3 MVP claim.

---

## Eight deliverables

| # | Deliverable | Status | Evidence |
|---|-------------|--------|----------|
| 1 | EffectIR | **pass** | closed TypeBox schema; translate/compile diagnostics; invalid IR receives no canonical hash |
| 2 | PathRef / SecretRef / ServiceRef | **pass** | typed refs; unsupported secret/service backends fail before body execution |
| 3 | labels | **pass** | compositional source/sink summaries cover `flow.def`, saved `use`, `expand`, and unresolved dynamic definitions in schema, verifier, compiler, and runtime |
| 4 | resource transaction | **pass** | durable snapshot/intent/permit + Commit-or-Restore + non-throwing post-terminal cleanup + process-crash recovery/GC |
| 5 | single mutation authority | **pass** | legacy changeset/gateway authority removed; effects bridge delegates to resources |
| 6 | mutating-path overlap | **pass** | phase-local static overlap + persistent cross-session/run lease admission |
| 7 | host matrix | **pass (honest)** | five hosts explicitly resolve-only; FileBroker/secret/service cells explicitly unsupported |
| 8 | why-* | **pass** | DAG-consistent explanation avoids invented independent-phase dependencies; ledger readback derives principal/capability/intent/generation |

---

## Wave log

| Wave | Result |
|------|--------|
| Resource convergence | single resources authority; focused + full contract PASS |
| Information flow | transitive DAG labels wired through validate/verify/compile/runtime; false-edge regression covered |
| Real host | live Codex three-agent run persisted final output through resource transaction; ledger authorization PASS |
| Remote candidate | Draft PR #117; run 31167592775 passed all CI jobs on `1478510f` |
| Post-review hardening | composition/cache and dynamic-spawn taint; nested information flow; malformed `effects`; terminal cleanup/lease safety; terminal/orphan before-image GC; local contract PASS on `60126954` |
| 0.3 RC pipeline | full RC pipeline green (logs `/tmp/taskflow-03-rc-final/`, built at evidence SHA `6071fb2`): typecheck, 2202-test suite 2198/0/4, build, test:pack 9 pkgs, built Codex MCP 16/16, audit --prod clean; store .pi discovery hardened and re-verified at exact SHA `96a32e8`; exact-SHA remote CI open |

## Explicitly deferred non-blocker

The append-only resource journal still lacks a sharded/compacted projection designed for thousands of intents, and long-lived control-file scale has not been benchmarked. That is a performance/capacity limitation, not a Commit-or-Reject exception in the current bounded MVP. Full before-image bodies are now removed after durable clean terminal states, and aged pre-intent orphans are collected at startup; no high-scale journal claim is made for 0.3.

---

## Commands last green

```text
pnpm run typecheck
# → PASS (re-run at exact SHA 96a32e8; t_bf5ab0c1)

pnpm test
# → 2202 tests: 2198 pass / 0 fail / 4 skipped  (RC pipeline log; built at evidence SHA 6071fb2)

pnpm run build
# → PASS across core, CharterArc, MCP, hosts, DSL, and five delivery packages (RC pipeline log)

pnpm run test:pack
# → PASS 9 packages, 24 explicit imports, 80 wildcard exports, 5 bins (RC pipeline log)

pnpm run test:e2e-codex-mcp-full
# → PASS — comprehensive e2e 16/16 against the built dist bin, incl. TE fixture:
#   fs.write committed through resources + ledger-backed why-effect (RC pipeline log;
#   evidence SHA 6071fb2 per build-info stamp)

pnpm audit --prod
# → no known vulnerabilities (RC pipeline log)

# Focused store suite at exact SHA 96a32e8 (t_bf5ab0c1):
# node --conditions=development --experimental-strip-types --test packages/taskflow-core/test/store.test.ts
# → 69/69 pass
# Full store*.test.ts glob (store, store-extended, store-dogfood) → 100/100 pass (re-verified)

# NOT RERUN on 96a32e8:
# pnpm test (full suite) / build / test:pack / test:e2e-codex-mcp-full — RC pipeline logs are at evidence SHA 6071fb2 (store delta re-verified at exact SHA)
# pnpm run test:e2e-codex  (live Codex CLI; prior-candidate historical A→B→C PASS only)
# Exact-SHA remote CI: open — branch pushed to origin but CI has no run (workflow triggers
# only on PR→main / push main; no PR head = 96a32e8; human gate: open/update PR).
```
